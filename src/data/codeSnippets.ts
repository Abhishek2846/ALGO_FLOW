export type SupportedLanguage = 'pseudocode' | 'python' | 'cpp' | 'java' | 'javascript';

export interface CodeSnippetMap {
  pseudocode: string[];
  python: string[];
  cpp: string[];
  java: string[];
  javascript: string[];
}

export const ALGO_SNIPPETS: Record<string, CodeSnippetMap> = {
  // ════════════════════════════════════════════════════
  //  SORTING ALGORITHMS
  // ════════════════════════════════════════════════════
  'bubble-sort': {
    pseudocode: [
      'procedure bubbleSort(A)',
      '  n ← length(A)',
      '  repeat',
      '    swapped ← false',
      '    for i ← 1 to n-1 do',
      '      if A[i-1] > A[i] then',
      '        swap(A[i-1], A[i])',
      '        swapped ← true',
      '      end if',
      '    end for',
      '    n ← n - 1',
      '  until not swapped',
      'end procedure',
    ],
    python: [
      'def bubble_sort(arr):',
      '    n = len(arr)',
      '    for i in range(n):',
      '        swapped = False',
      '        for j in range(0, n - i - 1):',
      '            if arr[j] > arr[j + 1]:',
      '                arr[j], arr[j + 1] = arr[j + 1], arr[j]',
      '                swapped = True',
      '        if not swapped:',
      '            break',
      '    return arr',
    ],
    cpp: [
      '#include <vector>',
      '#include <algorithm>',
      '',
      'void bubbleSort(std::vector<int>& arr) {',
      '    int n = arr.size();',
      '    for (int i = 0; i < n - 1; i++) {',
      '        bool swapped = false;',
      '        for (int j = 0; j < n - i - 1; j++) {',
      '            if (arr[j] > arr[j + 1]) {',
      '                std::swap(arr[j], arr[j + 1]);',
      '                swapped = true;',
      '            }',
      '        }',
      '        if (!swapped) break;',
      '    }',
      '}',
    ],
    java: [
      'public class BubbleSort {',
      '    public static void sort(int[] arr) {',
      '        int n = arr.length;',
      '        for (int i = 0; i < n - 1; i++) {',
      '            boolean swapped = false;',
      '            for (int j = 0; j < n - i - 1; j++) {',
      '                if (arr[j] > arr[j + 1]) {',
      '                    int temp = arr[j];',
      '                    arr[j] = arr[j + 1];',
      '                    arr[j + 1] = temp;',
      '                    swapped = true;',
      '                }',
      '            }',
      '            if (!swapped) break;',
      '        }',
      '    }',
      '}',
    ],
    javascript: [
      'function bubbleSort(arr) {',
      '  const n = arr.length;',
      '  for (let i = 0; i < n - 1; i++) {',
      '    let swapped = false;',
      '    for (let j = 0; j < n - i - 1; j++) {',
      '      if (arr[j] > arr[j + 1]) {',
      '        [arr[j], arr[j + 1]] = [arr[j + 1], arr[j]];',
      '        swapped = true;',
      '      }',
      '    }',
      '    if (!swapped) break;',
      '  }',
      '  return arr;',
      '}',
    ],
  },

  'selection-sort': {
    pseudocode: [
      'procedure selectionSort(A)',
      '  n ← length(A)',
      '  for i ← 0 to n-2 do',
      '    minIdx ← i',
      '    for j ← i+1 to n-1 do',
      '      if A[j] < A[minIdx] then',
      '        minIdx ← j',
      '      end if',
      '    end for',
      '    swap(A[i], A[minIdx])',
      '  end for',
      'end procedure',
    ],
    python: [
      'def selection_sort(arr):',
      '    n = len(arr)',
      '    for i in range(n - 1):',
      '        min_idx = i',
      '        for j in range(i + 1, n):',
      '            if arr[j] < arr[min_idx]:',
      '                min_idx = j',
      '        arr[i], arr[min_idx] = arr[min_idx], arr[i]',
      '    return arr',
    ],
    cpp: [
      '#include <vector>',
      '#include <algorithm>',
      '',
      'void selectionSort(std::vector<int>& arr) {',
      '    int n = arr.size();',
      '    for (int i = 0; i < n - 1; i++) {',
      '        int minIdx = i;',
      '        for (int j = i + 1; j < n; j++) {',
      '            if (arr[j] < arr[minIdx]) {',
      '                minIdx = j;',
      '            }',
      '        }',
      '        std::swap(arr[i], arr[minIdx]);',
      '    }',
      '}',
    ],
    java: [
      'public class SelectionSort {',
      '    public static void sort(int[] arr) {',
      '        int n = arr.length;',
      '        for (int i = 0; i < n - 1; i++) {',
      '            int minIdx = i;',
      '            for (int j = i + 1; j < n; j++) {',
      '                if (arr[j] < arr[minIdx]) {',
      '                    minIdx = j;',
      '                }',
      '            }',
      '            int temp = arr[minIdx];',
      '            arr[minIdx] = arr[i];',
      '            arr[i] = temp;',
      '        }',
      '    }',
      '}',
    ],
    javascript: [
      'function selectionSort(arr) {',
      '  const n = arr.length;',
      '  for (let i = 0; i < n - 1; i++) {',
      '    let minIdx = i;',
      '    for (let j = i + 1; j < n; j++) {',
      '      if (arr[j] < arr[minIdx]) {',
      '        minIdx = j;',
      '      }',
      '    }',
      '    if (minIdx !== i) {',
      '      [arr[i], arr[minIdx]] = [arr[minIdx], arr[i]];',
      '    }',
      '  }',
      '  return arr;',
      '}',
    ],
  },

  'insertion-sort': {
    pseudocode: [
      'procedure insertionSort(A)',
      '  for i ← 1 to length(A)-1 do',
      '    key ← A[i]',
      '    j ← i - 1',
      '    while j >= 0 and A[j] > key do',
      '      A[j+1] ← A[j]',
      '      j ← j - 1',
      '    end while',
      '    A[j+1] ← key',
      '  end for',
      'end procedure',
    ],
    python: [
      'def insertion_sort(arr):',
      '    for i in range(1, len(arr)):',
      '        key = arr[i]',
      '        j = i - 1',
      '        while j >= 0 and arr[j] > key:',
      '            arr[j + 1] = arr[j]',
      '            j -= 1',
      '        arr[j + 1] = key',
      '    return arr',
    ],
    cpp: [
      '#include <vector>',
      '',
      'void insertionSort(std::vector<int>& arr) {',
      '    int n = arr.size();',
      '    for (int i = 1; i < n; i++) {',
      '        int key = arr[i];',
      '        int j = i - 1;',
      '        while (j >= 0 && arr[j] > key) {',
      '            arr[j + 1] = arr[j];',
      '            j--;',
      '        }',
      '        arr[j + 1] = key;',
      '    }',
      '}',
    ],
    java: [
      'public class InsertionSort {',
      '    public static void sort(int[] arr) {',
      '        int n = arr.length;',
      '        for (int i = 1; i < n; i++) {',
      '            int key = arr[i];',
      '            int j = i - 1;',
      '            while (j >= 0 && arr[j] > key) {',
      '                arr[j + 1] = arr[j];',
      '                j--;',
      '            }',
      '            arr[j + 1] = key;',
      '        }',
      '    }',
      '}',
    ],
    javascript: [
      'function insertionSort(arr) {',
      '  for (let i = 1; i < arr.length; i++) {',
      '    const key = arr[i];',
      '    let j = i - 1;',
      '    while (j >= 0 && arr[j] > key) {',
      '      arr[j + 1] = arr[j];',
      '      j--;',
      '    }',
      '    arr[j + 1] = key;',
      '  }',
      '  return arr;',
      '}',
    ],
  },

  'merge-sort': {
    pseudocode: [
      'procedure mergeSort(A, lo, hi)',
      '  if lo >= hi then return',
      '  mid ← ⌊(lo + hi) / 2⌋',
      '  mergeSort(A, lo, mid)',
      '  mergeSort(A, mid+1, hi)',
      '  merge(A, lo, mid, hi)',
      'end procedure',
    ],
    python: [
      'def merge_sort(arr):',
      '    if len(arr) <= 1:',
      '        return arr',
      '    mid = len(arr) // 2',
      '    left = merge_sort(arr[:mid])',
      '    right = merge_sort(arr[mid:])',
      '    return merge(left, right)',
      '',
      'def merge(left, right):',
      '    result = []',
      '    i = j = 0',
      '    while i < len(left) and j < len(right):',
      '        if left[i] <= right[j]:',
      '            result.append(left[i]); i += 1',
      '        else:',
      '            result.append(right[j]); j += 1',
      '    result.extend(left[i:])',
      '    result.extend(right[j:])',
      '    return result',
    ],
    cpp: [
      '#include <vector>',
      '',
      'void merge(std::vector<int>& arr, int l, int m, int r) {',
      '    std::vector<int> left(arr.begin() + l, arr.begin() + m + 1);',
      '    std::vector<int> right(arr.begin() + m + 1, arr.begin() + r + 1);',
      '    int i = 0, j = 0, k = l;',
      '    while (i < left.size() && j < right.size()) {',
      '        if (left[i] <= right[j]) arr[k++] = left[i++];',
      '        else arr[k++] = right[j++];',
      '    }',
      '    while (i < left.size()) arr[k++] = left[i++];',
      '    while (j < right.size()) arr[k++] = right[j++];',
      '}',
      '',
      'void mergeSort(std::vector<int>& arr, int l, int r) {',
      '    if (l >= r) return;',
      '    int m = l + (r - l) / 2;',
      '    mergeSort(arr, l, m);',
      '    mergeSort(arr, m + 1, r);',
      '    merge(arr, l, m, r);',
      '}',
    ],
    java: [
      'public class MergeSort {',
      '    public static void sort(int[] arr, int l, int r) {',
      '        if (l >= r) return;',
      '        int m = l + (r - l) / 2;',
      '        sort(arr, l, m);',
      '        sort(arr, m + 1, r);',
      '        merge(arr, l, m, r);',
      '    }',
      '',
      '    private static void merge(int[] arr, int l, int m, int r) {',
      '        int[] left = java.util.Arrays.copyOfRange(arr, l, m + 1);',
      '        int[] right = java.util.Arrays.copyOfRange(arr, m + 1, r + 1);',
      '        int i = 0, j = 0, k = l;',
      '        while (i < left.length && j < right.length) {',
      '            arr[k++] = (left[i] <= right[j]) ? left[i++] : right[j++];',
      '        }',
      '        while (i < left.length) arr[k++] = left[i++];',
      '        while (j < right.length) arr[k++] = right[j++];',
      '    }',
      '}',
    ],
    javascript: [
      'function mergeSort(arr) {',
      '  if (arr.length <= 1) return arr;',
      '  const mid = Math.floor(arr.length / 2);',
      '  const left = mergeSort(arr.slice(0, mid));',
      '  const right = mergeSort(arr.slice(mid));',
      '  return merge(left, right);',
      '}',
      '',
      'function merge(left, right) {',
      '  const res = [];',
      '  let i = 0, j = 0;',
      '  while (i < left.length && j < right.length) {',
      '    if (left[i] <= right[j]) res.push(left[i++]);',
      '    else res.push(right[j++]);',
      '  }',
      '  return res.concat(left.slice(i)).concat(right.slice(j));',
      '}',
    ],
  },

  'quick-sort': {
    pseudocode: [
      'procedure quickSort(A, lo, hi)',
      '  if lo < hi then',
      '    p ← partition(A, lo, hi)',
      '    quickSort(A, lo, p-1)',
      '    quickSort(A, p+1, hi)',
      '  end if',
      'end procedure',
    ],
    python: [
      'def quick_sort(arr, low, high):',
      '    if low < high:',
      '        p = partition(arr, low, high)',
      '        quick_sort(arr, low, p - 1)',
      '        quick_sort(arr, p + 1, high)',
      '',
      'def partition(arr, low, high):',
      '    pivot = arr[high]',
      '    i = low - 1',
      '    for j in range(low, high):',
      '        if arr[j] <= pivot:',
      '            i += 1',
      '            arr[i], arr[j] = arr[j], arr[i]',
      '    arr[i + 1], arr[high] = arr[high], arr[i + 1]',
      '    return i + 1',
    ],
    cpp: [
      'int partition(std::vector<int>& arr, int low, int high) {',
      '    int pivot = arr[high];',
      '    int i = low - 1;',
      '    for (int j = low; j < high; j++) {',
      '        if (arr[j] <= pivot) {',
      '            i++;',
      '            std::swap(arr[i], arr[j]);',
      '        }',
      '    }',
      '    std::swap(arr[i + 1], arr[high]);',
      '    return i + 1;',
      '}',
      '',
      'void quickSort(std::vector<int>& arr, int low, int high) {',
      '    if (low < high) {',
      '        int p = partition(arr, low, high);',
      '        quickSort(arr, low, p - 1);',
      '        quickSort(arr, p + 1, high);',
      '    }',
      '}',
    ],
    java: [
      'public class QuickSort {',
      '    public static void sort(int[] arr, int low, int high) {',
      '        if (low < high) {',
      '            int p = partition(arr, low, high);',
      '            sort(arr, low, p - 1);',
      '            sort(arr, p + 1, high);',
      '        }',
      '    }',
      '',
      '    private static int partition(int[] arr, int low, int high) {',
      '        int pivot = arr[high];',
      '        int i = low - 1;',
      '        for (int j = low; j < high; j++) {',
      '            if (arr[j] <= pivot) {',
      '                i++;',
      '                int temp = arr[i]; arr[i] = arr[j]; arr[j] = temp;',
      '            }',
      '        }',
      '        int temp = arr[i + 1]; arr[i + 1] = arr[high]; arr[high] = temp;',
      '        return i + 1;',
      '    }',
      '}',
    ],
    javascript: [
      'function quickSort(arr, low = 0, high = arr.length - 1) {',
      '  if (low < high) {',
      '    const p = partition(arr, low, high);',
      '    quickSort(arr, low, p - 1);',
      '    quickSort(arr, p + 1, high);',
      '  }',
      '  return arr;',
      '}',
      '',
      'function partition(arr, low, high) {',
      '  const pivot = arr[high];',
      '  let i = low - 1;',
      '  for (let j = low; j < high; j++) {',
      '    if (arr[j] <= pivot) {',
      '      i++;',
      '      [arr[i], arr[j]] = [arr[j], arr[i]];',
      '    }',
      '  }',
      '  [arr[i + 1], arr[high]] = [arr[high], arr[i + 1]];',
      '  return i + 1;',
      '}',
    ],
  },

  'heap-sort': {
    pseudocode: [
      'procedure heapSort(A)',
      '  buildMaxHeap(A)',
      '  for i ← length(A)-1 downto 1 do',
      '    swap(A[0], A[i])',
      '    heapify(A, 0, i)',
      '  end for',
      'end procedure',
    ],
    python: [
      'def heapify(arr, n, i):',
      '    largest = i',
      '    left = 2 * i + 1',
      '    right = 2 * i + 2',
      '    if left < n and arr[left] > arr[largest]: largest = left',
      '    if right < n and arr[right] > arr[largest]: largest = right',
      '    if largest != i:',
      '        arr[i], arr[largest] = arr[largest], arr[i]',
      '        heapify(arr, n, largest)',
      '',
      'def heap_sort(arr):',
      '    n = len(arr)',
      '    for i in range(n // 2 - 1, -1, -1):',
      '        heapify(arr, n, i)',
      '    for i in range(n - 1, 0, -1):',
      '        arr[i], arr[0] = arr[0], arr[i]',
      '        heapify(arr, i, 0)',
      '    return arr',
    ],
    cpp: [
      '#include <vector>',
      '#include <algorithm>',
      '',
      'void heapify(std::vector<int>& arr, int n, int i) {',
      '    int largest = i;',
      '    int l = 2 * i + 1;',
      '    int r = 2 * i + 2;',
      '    if (l < n && arr[l] > arr[largest]) largest = l;',
      '    if (r < n && arr[r] > arr[largest]) largest = r;',
      '    if (largest != i) {',
      '        std::swap(arr[i], arr[largest]);',
      '        heapify(arr, n, largest);',
      '    }',
      '}',
      '',
      'void heapSort(std::vector<int>& arr) {',
      '    int n = arr.size();',
      '    for (int i = n / 2 - 1; i >= 0; i--) heapify(arr, n, i);',
      '    for (int i = n - 1; i > 0; i--) {',
      '        std::swap(arr[0], arr[i]);',
      '        heapify(arr, i, 0);',
      '    }',
      '}',
    ],
    java: [
      'public class HeapSort {',
      '    public static void sort(int[] arr) {',
      '        int n = arr.length;',
      '        for (int i = n / 2 - 1; i >= 0; i--) heapify(arr, n, i);',
      '        for (int i = n - 1; i > 0; i--) {',
      '            int temp = arr[0]; arr[0] = arr[i]; arr[i] = temp;',
      '            heapify(arr, i, 0);',
      '        }',
      '    }',
      '',
      '    private static void heapify(int[] arr, int n, int i) {',
      '        int largest = i;',
      '        int l = 2 * i + 1, r = 2 * i + 2;',
      '        if (l < n && arr[l] > arr[largest]) largest = l;',
      '        if (r < n && arr[r] > arr[largest]) largest = r;',
      '        if (largest != i) {',
      '            int temp = arr[i]; arr[i] = arr[largest]; arr[largest] = temp;',
      '            heapify(arr, n, largest);',
      '        }',
      '    }',
      '}',
    ],
    javascript: [
      'function heapify(arr, n, i) {',
      '  let largest = i;',
      '  const l = 2 * i + 1, r = 2 * i + 2;',
      '  if (l < n && arr[l] > arr[largest]) largest = l;',
      '  if (r < n && arr[r] > arr[largest]) largest = r;',
      '  if (largest !== i) {',
      '    [arr[i], arr[largest]] = [arr[largest], arr[i]];',
      '    heapify(arr, n, largest);',
      '  }',
      '}',
      '',
      'function heapSort(arr) {',
      '  const n = arr.length;',
      '  for (let i = Math.floor(n / 2) - 1; i >= 0; i--) heapify(arr, n, i);',
      '  for (let i = n - 1; i > 0; i--) {',
      '    [arr[0], arr[i]] = [arr[i], arr[0]];',
      '    heapify(arr, i, 0);',
      '  }',
      '  return arr;',
      '}',
    ],
  },

  'counting-sort': {
    pseudocode: [
      'procedure countingSort(A, k)',
      '  count ← array of zeros, size k+1',
      '  for each x in A do count[x] ← count[x] + 1',
      '  for i ← 1 to k do count[i] ← count[i] + count[i-1]',
      '  output ← array of size n',
      '  for j ← n-1 downto 0 do',
      '    output[count[A[j]]-1] ← A[j]',
      '    count[A[j]] ← count[A[j]] - 1',
      '  end for',
      '  return output',
      'end procedure',
    ],
    python: [
      'def counting_sort(arr):',
      '    if not arr: return arr',
      '    max_val = max(arr)',
      '    count = [0] * (max_val + 1)',
      '    for num in arr:',
      '        count[num] += 1',
      '    sorted_idx = 0',
      '    for val, cnt in enumerate(count):',
      '        for _ in range(cnt):',
      '            arr[sorted_idx] = val',
      '            sorted_idx += 1',
      '    return arr',
    ],
    cpp: [
      '#include <vector>',
      '#include <algorithm>',
      '',
      'void countingSort(std::vector<int>& arr) {',
      '    if (arr.empty()) return;',
      '    int maxVal = *std::max_element(arr.begin(), arr.end());',
      '    std::vector<int> count(maxVal + 1, 0);',
      '    for (int num : arr) count[num]++;',
      '    int idx = 0;',
      '    for (int i = 0; i <= maxVal; i++) {',
      '        while (count[i]-- > 0) arr[idx++] = i;',
      '    }',
      '}',
    ],
    java: [
      'public class CountingSort {',
      '    public static void sort(int[] arr) {',
      '        if (arr.length == 0) return;',
      '        int max = arr[0];',
      '        for (int v : arr) if (v > max) max = v;',
      '        int[] count = new int[max + 1];',
      '        for (int v : arr) count[v]++;',
      '        int idx = 0;',
      '        for (int i = 0; i <= max; i++) {',
      '            while (count[i]-- > 0) arr[idx++] = i;',
      '        }',
      '    }',
      '}',
    ],
    javascript: [
      'function countingSort(arr) {',
      '  if (arr.length === 0) return arr;',
      '  const max = Math.max(...arr);',
      '  const count = new Array(max + 1).fill(0);',
      '  for (const num of arr) count[num]++;',
      '  let idx = 0;',
      '  for (let i = 0; i <= max; i++) {',
      '    while (count[i] > 0) {',
      '      arr[idx++] = i;',
      '      count[i]--;',
      '    }',
      '  }',
      '  return arr;',
      '}',
    ],
  },

  'radix-sort': {
    pseudocode: [
      'procedure radixSort(A)',
      '  max ← maximum value in A',
      '  exp ← 1',
      '  while max / exp > 0 do',
      '    countingSortByDigit(A, exp)',
      '    exp ← exp * 10',
      '  end while',
      'end procedure',
    ],
    python: [
      'def counting_sort_digit(arr, exp):',
      '    n = len(arr)',
      '    output = [0] * n',
      '    count = [0] * 10',
      '    for i in range(n):',
      '        idx = (arr[i] // exp) % 10',
      '        count[idx] += 1',
      '    for i in range(1, 10):',
      '        count[i] += count[i - 1]',
      '    for i in range(n - 1, -1, -1):',
      '        idx = (arr[i] // exp) % 10',
      '        output[count[idx] - 1] = arr[i]',
      '        count[idx] -= 1',
      '    for i in range(n): arr[i] = output[i]',
      '',
      'def radix_sort(arr):',
      '    max_val = max(arr)',
      '    exp = 1',
      '    while max_val // exp > 0:',
      '        counting_sort_digit(arr, exp)',
      '        exp *= 10',
      '    return arr',
    ],
    cpp: [
      '#include <vector>',
      '#include <algorithm>',
      '',
      'void countSortByDigit(std::vector<int>& arr, int exp) {',
      '    int n = arr.size();',
      '    std::vector<int> output(n);',
      '    int count[10] = {0};',
      '    for (int i = 0; i < n; i++) count[(arr[i] / exp) % 10]++;',
      '    for (int i = 1; i < 10; i++) count[i] += count[i - 1];',
      '    for (int i = n - 1; i >= 0; i--) {',
      '        output[count[(arr[i] / exp) % 10] - 1] = arr[i];',
      '        count[(arr[i] / exp) % 10]--;',
      '    }',
      '    for (int i = 0; i < n; i++) arr[i] = output[i];',
      '}',
      '',
      'void radixSort(std::vector<int>& arr) {',
      '    if (arr.empty()) return;',
      '    int maxVal = *std::max_element(arr.begin(), arr.end());',
      '    for (int exp = 1; maxVal / exp > 0; exp *= 10)',
      '        countSortByDigit(arr, exp);',
      '}',
    ],
    java: [
      'public class RadixSort {',
      '    public static void sort(int[] arr) {',
      '        if (arr.length == 0) return;',
      '        int max = arr[0];',
      '        for (int v : arr) if (v > max) max = v;',
      '        for (int exp = 1; max / exp > 0; exp *= 10)',
      '            countSortByDigit(arr, exp);',
      '    }',
      '',
      '    private static void countSortByDigit(int[] arr, int exp) {',
      '        int n = arr.length;',
      '        int[] output = new int[n];',
      '        int[] count = new int[10];',
      '        for (int i = 0; i < n; i++) count[(arr[i] / exp) % 10]++;',
      '        for (int i = 1; i < 10; i++) count[i] += count[i - 1];',
      '        for (int i = n - 1; i >= 0; i--) {',
      '            output[count[(arr[i] / exp) % 10] - 1] = arr[i];',
      '            count[(arr[i] / exp) % 10]--;',
      '        }',
      '        System.arraycopy(output, 0, arr, 0, n);',
      '    }',
      '}',
    ],
    javascript: [
      'function radixSort(arr) {',
      '  if (arr.length === 0) return arr;',
      '  const max = Math.max(...arr);',
      '  for (let exp = 1; Math.floor(max / exp) > 0; exp *= 10) {',
      '    countingSortDigit(arr, exp);',
      '  }',
      '  return arr;',
      '}',
      '',
      'function countingSortDigit(arr, exp) {',
      '  const n = arr.length;',
      '  const output = new Array(n);',
      '  const count = new Array(10).fill(0);',
      '  for (let i = 0; i < n; i++) count[Math.floor(arr[i] / exp) % 10]++;',
      '  for (let i = 1; i < 10; i++) count[i] += count[i - 1];',
      '  for (let i = n - 1; i >= 0; i--) {',
      '    const digit = Math.floor(arr[i] / exp) % 10;',
      '    output[count[digit] - 1] = arr[i];',
      '    count[digit]--;',
      '  }',
      '  for (let i = 0; i < n; i++) arr[i] = output[i];',
      '}',
    ],
  },

  // ════════════════════════════════════════════════════
  //  SEARCHING ALGORITHMS
  // ════════════════════════════════════════════════════
  'linear-search': {
    pseudocode: [
      'procedure linearSearch(A, target)',
      '  for i ← 0 to length(A)-1 do',
      '    if A[i] = target then',
      '      return i',
      '    end if',
      '  end for',
      '  return -1',
      'end procedure',
    ],
    python: [
      'def linear_search(arr, target):',
      '    for i in range(len(arr)):',
      '        if arr[i] == target:',
      '            return i  # Target found at index i',
      '    return -1  # Target not found in list',
    ],
    cpp: [
      '#include <vector>',
      '',
      'int linearSearch(const std::vector<int>& arr, int target) {',
      '    for (size_t i = 0; i < arr.size(); i++) {',
      '        if (arr[i] == target) {',
      '            return static_cast<int>(i);',
      '        }',
      '    }',
      '    return -1;',
      '}',
    ],
    java: [
      'public class LinearSearch {',
      '    public static int search(int[] arr, int target) {',
      '        for (int i = 0; i < arr.length; i++) {',
      '            if (arr[i] == target) {',
      '                return i;',
      '            }',
      '        }',
      '        return -1;',
      '    }',
      '}',
    ],
    javascript: [
      'function linearSearch(arr, target) {',
      '  for (let i = 0; i < arr.length; i++) {',
      '    if (arr[i] === target) {',
      '      return i;',
      '    }',
      '  }',
      '  return -1;',
      '}',
    ],
  },

  'binary-search': {
    pseudocode: [
      'procedure binarySearch(A, target)',
      '  low ← 0',
      '  high ← length(A) - 1',
      '  while low <= high do',
      '    mid ← ⌊(low + high) / 2⌋',
      '    if A[mid] = target then return mid',
      '    else if A[mid] < target then low ← mid + 1',
      '    else high ← mid - 1',
      '  end while',
      '  return -1',
      'end procedure',
    ],
    python: [
      'def binary_search(arr, target):',
      '    low = 0',
      '    high = len(arr) - 1',
      '    while low <= high:',
      '        mid = (low + high) // 2',
      '        if arr[mid] == target:',
      '            return mid',
      '        elif arr[mid] < target:',
      '            low = mid + 1',
      '        else:',
      '            high = mid - 1',
      '    return -1',
    ],
    cpp: [
      '#include <vector>',
      '',
      'int binarySearch(const std::vector<int>& arr, int target) {',
      '    int low = 0;',
      '    int high = arr.size() - 1;',
      '    while (low <= high) {',
      '        int mid = low + (high - low) / 2;',
      '        if (arr[mid] == target) return mid;',
      '        if (arr[mid] < target) low = mid + 1;',
      '        else high = mid - 1;',
      '    }',
      '    return -1;',
      '}',
    ],
    java: [
      'public class BinarySearch {',
      '    public static int search(int[] arr, int target) {',
      '        int low = 0;',
      '        int high = arr.length - 1;',
      '        while (low <= high) {',
      '            int mid = low + (high - low) / 2;',
      '            if (arr[mid] == target) return mid;',
      '            if (arr[mid] < target) low = mid + 1;',
      '            else high = mid - 1;',
      '        }',
      '        return -1;',
      '    }',
      '}',
    ],
    javascript: [
      'function binarySearch(arr, target) {',
      '  let low = 0;',
      '  let high = arr.length - 1;',
      '  while (low <= high) {',
      '    const mid = Math.floor((low + high) / 2);',
      '    if (arr[mid] === target) return mid;',
      '    if (arr[mid] < target) low = mid + 1;',
      '    else high = mid - 1;',
      '  }',
      '  return -1;',
      '}',
    ],
  },

  'jump-search': {
    pseudocode: [
      'procedure jumpSearch(A, target)',
      '  n ← length(A)',
      '  step ← ⌊√n⌋',
      '  prev ← 0',
      '  while A[min(step, n)-1] < target do',
      '    prev ← step',
      '    step ← step + ⌊√n⌋',
      '    if prev >= n then return -1',
      '  end while',
      '  while A[prev] < target do',
      '    prev ← prev + 1',
      '    if prev = min(step, n) then return -1',
      '  end while',
      '  if A[prev] = target then return prev',
      '  return -1',
      'end procedure',
    ],
    python: [
      'import math',
      '',
      'def jump_search(arr, target):',
      '    n = len(arr)',
      '    step = int(math.sqrt(n))',
      '    prev = 0',
      '    while arr[min(step, n) - 1] < target:',
      '        prev = step',
      '        step += int(math.sqrt(n))',
      '        if prev >= n:',
      '            return -1',
      '    while arr[prev] < target:',
      '        prev += 1',
      '        if prev == min(step, n):',
      '            return -1',
      '    if arr[prev] == target:',
      '        return prev',
      '    return -1',
    ],
    cpp: [
      '#include <vector>',
      '#include <cmath>',
      '#include <algorithm>',
      '',
      'int jumpSearch(const std::vector<int>& arr, int target) {',
      '    int n = arr.size();',
      '    int step = std::sqrt(n);',
      '    int prev = 0;',
      '    while (arr[std::min(step, n) - 1] < target) {',
      '        prev = step;',
      '        step += std::sqrt(n);',
      '        if (prev >= n) return -1;',
      '    }',
      '    while (arr[prev] < target) {',
      '        prev++;',
      '        if (prev == std::min(step, n)) return -1;',
      '    }',
      '    if (arr[prev] == target) return prev;',
      '    return -1;',
      '}',
    ],
    java: [
      'public class JumpSearch {',
      '    public static int search(int[] arr, int target) {',
      '        int n = arr.length;',
      '        int step = (int) Math.floor(Math.sqrt(n));',
      '        int prev = 0;',
      '        while (arr[Math.min(step, n) - 1] < target) {',
      '            prev = step;',
      '            step += (int) Math.floor(Math.sqrt(n));',
      '            if (prev >= n) return -1;',
      '        }',
      '        while (arr[prev] < target) {',
      '            prev++;',
      '            if (prev == Math.min(step, n)) return -1;',
      '        }',
      '        if (arr[prev] == target) return prev;',
      '        return -1;',
      '    }',
      '}',
    ],
    javascript: [
      'function jumpSearch(arr, target) {',
      '  const n = arr.length;',
      '  const stepSize = Math.floor(Math.sqrt(n));',
      '  let step = stepSize;',
      '  let prev = 0;',
      '  while (arr[Math.min(step, n) - 1] < target) {',
      '    prev = step;',
      '    step += stepSize;',
      '    if (prev >= n) return -1;',
      '  }',
      '  while (arr[prev] < target) {',
      '    prev++;',
      '    if (prev === Math.min(step, n)) return -1;',
      '  }',
      '  if (arr[prev] === target) return prev;',
      '  return -1;',
      '}',
    ],
  },

  'interpolation-search': {
    pseudocode: [
      'procedure interpolationSearch(A, target)',
      '  low ← 0, high ← length(A) - 1',
      '  while low <= high and target >= A[low] and target <= A[high] do',
      '    pos ← low + ⌊((target - A[low]) * (high - low)) / (A[high] - A[low])⌋',
      '    if A[pos] = target then return pos',
      '    if A[pos] < target then low ← pos + 1',
      '    else high ← pos - 1',
      '  end while',
      '  return -1',
      'end procedure',
    ],
    python: [
      'def interpolation_search(arr, target):',
      '    low = 0',
      '    high = len(arr) - 1',
      '    while low <= high and arr[low] <= target <= arr[high]:',
      '        if low == high:',
      '            if arr[low] == target: return low',
      '            return -1',
      '        pos = low + int(((target - arr[low]) * (high - low)) / (arr[high] - arr[low]))',
      '        if arr[pos] == target: return pos',
      '        if arr[pos] < target:',
      '            low = pos + 1',
      '        else:',
      '            high = pos - 1',
      '    return -1',
    ],
    cpp: [
      '#include <vector>',
      '',
      'int interpolationSearch(const std::vector<int>& arr, int target) {',
      '    int low = 0, high = arr.size() - 1;',
      '    while (low <= high && target >= arr[low] && target <= arr[high]) {',
      '        if (low == high) {',
      '            if (arr[low] == target) return low;',
      '            return -1;',
      '        }',
      '        int pos = low + (((double)(high - low) / (arr[high] - arr[low])) * (target - arr[low]));',
      '        if (arr[pos] == target) return pos;',
      '        if (arr[pos] < target) low = pos + 1;',
      '        else high = pos - 1;',
      '    }',
      '    return -1;',
      '}',
    ],
    java: [
      'public class InterpolationSearch {',
      '    public static int search(int[] arr, int target) {',
      '        int low = 0, high = arr.length - 1;',
      '        while (low <= high && target >= arr[low] && target <= arr[high]) {',
      '            if (low == high) {',
      '                if (arr[low] == target) return low;',
      '                return -1;',
      '            }',
      '            int pos = low + (int)(((double)(high - low) / (arr[high] - arr[low])) * (target - arr[low]));',
      '            if (arr[pos] == target) return pos;',
      '            if (arr[pos] < target) low = pos + 1;',
      '            else high = pos - 1;',
      '        }',
      '        return -1;',
      '    }',
      '}',
    ],
    javascript: [
      'function interpolationSearch(arr, target) {',
      '  let low = 0;',
      '  let high = arr.length - 1;',
      '  while (low <= high && target >= arr[low] && target <= arr[high]) {',
      '    if (low === high) {',
      '      if (arr[low] === target) return low;',
      '      return -1;',
      '    }',
      '    const pos = low + Math.floor(((target - arr[low]) * (high - low)) / (arr[high] - arr[low]));',
      '    if (arr[pos] === target) return pos;',
      '    if (arr[pos] < target) low = pos + 1;',
      '    else high = pos - 1;',
      '  }',
      '  return -1;',
      '}',
    ],
  },

  // ════════════════════════════════════════════════════
  //  TREE ALGORITHMS
  // ════════════════════════════════════════════════════
  'binary-tree': {
    pseudocode: [
      'procedure insertBT(root, val)',
      '  newNode ← Node(val)',
      '  if root is null then return newNode',
      '  Q ← queue containing root',
      '  while Q is not empty do',
      '    node ← Q.dequeue()',
      '    if node.left is null then node.left ← newNode; break',
      '    else Q.enqueue(node.left)',
      '    if node.right is null then node.right ← newNode; break',
      '    else Q.enqueue(node.right)',
      '  end while',
      'end procedure',
    ],
    python: [
      'from collections import deque',
      '',
      'class Node:',
      '    def __init__(self, val):',
      '        self.val = val',
      '        self.left = None',
      '        self.right = None',
      '',
      'def insert_level_order(root, val):',
      '    new_node = Node(val)',
      '    if not root: return new_node',
      '    q = deque([root])',
      '    while q:',
      '        node = q.popleft()',
      '        if not node.left:',
      '            node.left = new_node; break',
      '        else: q.append(node.left)',
      '        if not node.right:',
      '            node.right = new_node; break',
      '        else: q.append(node.right)',
      '    return root',
    ],
    cpp: [
      '#include <iostream>',
      '#include <queue>',
      '',
      'struct Node {',
      '    int val;',
      '    Node* left;',
      '    Node* right;',
      '    Node(int v) : val(v), left(nullptr), right(nullptr) {}',
      '};',
      '',
      'Node* insertLevelOrder(Node* root, int val) {',
      '    Node* newNode = new Node(val);',
      '    if (!root) return newNode;',
      '    std::queue<Node*> q;',
      '    q.push(root);',
      '    while (!q.empty()) {',
      '        Node* curr = q.front(); q.pop();',
      '        if (!curr->left) { curr->left = newNode; break; }',
      '        else q.push(curr->left);',
      '        if (!curr->right) { curr->right = newNode; break; }',
      '        else q.push(curr->right);',
      '    }',
      '    return root;',
      '}',
    ],
    java: [
      'import java.util.LinkedList;',
      'import java.util.Queue;',
      '',
      'class Node {',
      '    int val;',
      '    Node left, right;',
      '    Node(int v) { val = v; }',
      '}',
      '',
      'public class BinaryTree {',
      '    public static Node insert(Node root, int val) {',
      '        Node newNode = new Node(val);',
      '        if (root == null) return newNode;',
      '        Queue<Node> q = new LinkedList<>();',
      '        q.add(root);',
      '        while (!q.isEmpty()) {',
      '            Node curr = q.poll();',
      '            if (curr.left == null) { curr.left = newNode; break; }',
      '            else q.add(curr.left);',
      '            if (curr.right == null) { curr.right = newNode; break; }',
      '            else q.add(curr.right);',
      '        }',
      '        return root;',
      '    }',
      '}',
    ],
    javascript: [
      'class Node {',
      '  constructor(val) {',
      '    this.val = val;',
      '    this.left = null;',
      '    this.right = null;',
      '  }',
      '}',
      '',
      'function insertLevelOrder(root, val) {',
      '  const newNode = new Node(val);',
      '  if (!root) return newNode;',
      '  const q = [root];',
      '  while (q.length > 0) {',
      '    const curr = q.shift();',
      '    if (!curr.left) { curr.left = newNode; break; }',
      '    else q.push(curr.left);',
      '    if (!curr.right) { curr.right = newNode; break; }',
      '    else q.push(curr.right);',
      '  }',
      '  return root;',
      '}',
    ],
  },

  'bst': {
    pseudocode: [
      'procedure insertBST(root, val)',
      '  if root is null then return Node(val)',
      '  if val < root.val then',
      '    root.left ← insertBST(root.left, val)',
      '  else if val > root.val then',
      '    root.right ← insertBST(root.right, val)',
      '  return root',
      'end procedure',
    ],
    python: [
      'class TreeNode:',
      '    def __init__(self, val):',
      '        self.val = val',
      '        self.left = None',
      '        self.right = None',
      '',
      'def insert_bst(root, val):',
      '    if not root:',
      '        return TreeNode(val)',
      '    if val < root.val:',
      '        root.left = insert_bst(root.left, val)',
      '    elif val > root.val:',
      '        root.right = insert_bst(root.right, val)',
      '    return root',
      '',
      'def search_bst(root, val):',
      '    if not root or root.val == val:',
      '        return root',
      '    if val < root.val:',
      '        return search_bst(root.left, val)',
      '    return search_bst(root.right, val)',
    ],
    cpp: [
      'struct TreeNode {',
      '    int val;',
      '    TreeNode* left;',
      '    TreeNode* right;',
      '    TreeNode(int v) : val(v), left(nullptr), right(nullptr) {}',
      '};',
      '',
      'TreeNode* insertBST(TreeNode* root, int val) {',
      '    if (!root) return new TreeNode(val);',
      '    if (val < root->val) root->left = insertBST(root->left, val);',
      '    else if (val > root->val) root->right = insertBST(root->right, val);',
      '    return root;',
      '}',
      '',
      'TreeNode* searchBST(TreeNode* root, int val) {',
      '    if (!root || root->val == val) return root;',
      '    if (val < root->val) return searchBST(root->left, val);',
      '    return searchBST(root->right, val);',
      '}',
    ],
    java: [
      'class TreeNode {',
      '    int val;',
      '    TreeNode left, right;',
      '    TreeNode(int v) { val = v; }',
      '}',
      '',
      'public class BST {',
      '    public static TreeNode insert(TreeNode root, int val) {',
      '        if (root == null) return new TreeNode(val);',
      '        if (val < root.val) root.left = insert(root.left, val);',
      '        else if (val > root.val) root.right = insert(root.right, val);',
      '        return root;',
      '    }',
      '',
      '    public static TreeNode search(TreeNode root, int val) {',
      '        if (root == null || root.val == val) return root;',
      '        if (val < root.val) return search(root.left, val);',
      '        return search(root.right, val);',
      '    }',
      '}',
    ],
    javascript: [
      'class TreeNode {',
      '  constructor(val) {',
      '    this.val = val;',
      '    this.left = null;',
      '    this.right = null;',
      '  }',
      '}',
      '',
      'function insertBST(root, val) {',
      '  if (!root) return new TreeNode(val);',
      '  if (val < root.val) root.left = insertBST(root.left, val);',
      '  else if (val > root.val) root.right = insertBST(root.right, val);',
      '  return root;',
      '}',
      '',
      'function searchBST(root, val) {',
      '  if (!root || root.val === val) return root;',
      '  if (val < root.val) return searchBST(root.left, val);',
      '  return searchBST(root.right, val);',
      '}',
    ],
  },

  'avl-tree': {
    pseudocode: [
      'procedure insertAVL(node, key)',
      '  if node is null then return Node(key)',
      '  if key < node.key then node.left ← insertAVL(node.left, key)',
      '  else if key > node.key then node.right ← insertAVL(node.right, key)',
      '  else return node',
      '  updateHeight(node)',
      '  b ← getBalance(node)',
      '  if b > 1 and key < node.left.key then return rotateRight(node)',
      '  if b < -1 and key > node.right.key then return rotateLeft(node)',
      '  if b > 1 and key > node.left.key then',
      '    node.left ← rotateLeft(node.left)',
      '    return rotateRight(node)',
      '  end if',
      '  if b < -1 and key < node.right.key then',
      '    node.right ← rotateRight(node.right)',
      '    return rotateLeft(node)',
      '  end if',
      '  return node',
      'end procedure',
    ],
    python: [
      'class Node:',
      '    def __init__(self, key):',
      '        self.key = key',
      '        self.left = None',
      '        self.right = None',
      '        self.height = 1',
      '',
      'def get_height(node): return node.height if node else 0',
      'def get_balance(node): return get_height(node.left) - get_height(node.right) if node else 0',
      '',
      'def rotate_right(y):',
      '    x = y.left; T2 = x.right',
      '    x.right = y; y.left = T2',
      '    y.height = 1 + max(get_height(y.left), get_height(y.right))',
      '    x.height = 1 + max(get_height(x.left), get_height(x.right))',
      '    return x',
      '',
      'def rotate_left(x):',
      '    y = x.right; T2 = y.left',
      '    y.left = x; x.right = T2',
      '    x.height = 1 + max(get_height(x.left), get_height(x.right))',
      '    y.height = 1 + max(get_height(y.left), get_height(y.right))',
      '    return y',
      '',
      'def insert_avl(root, key):',
      '    if not root: return Node(key)',
      '    if key < root.key: root.left = insert_avl(root.left, key)',
      '    elif key > root.key: root.right = insert_avl(root.right, key)',
      '    else: return root',
      '    root.height = 1 + max(get_height(root.left), get_height(root.right))',
      '    balance = get_balance(root)',
      '    if balance > 1 and key < root.left.key: return rotate_right(root)',
      '    if balance < -1 and key > root.right.key: return rotate_left(root)',
      '    if balance > 1 and key > root.left.key:',
      '        root.left = rotate_left(root.left); return rotate_right(root)',
      '    if balance < -1 and key < root.right.key:',
      '        root.right = rotate_right(root.right); return rotate_left(root)',
      '    return root',
    ],
    cpp: [
      '#include <iostream>',
      '#include <algorithm>',
      '',
      'struct Node {',
      '    int key, height;',
      '    Node *left, *right;',
      '    Node(int k) : key(k), height(1), left(nullptr), right(nullptr) {}',
      '};',
      '',
      'int height(Node* n) { return n ? n->height : 0; }',
      'int getBalance(Node* n) { return n ? height(n->left) - height(n->right) : 0; }',
      '',
      'Node* rotateRight(Node* y) {',
      '    Node* x = y->left; Node* T2 = x->right;',
      '    x->right = y; y->left = T2;',
      '    y->height = 1 + std::max(height(y->left), height(y->right));',
      '    x->height = 1 + std::max(height(x->left), height(x->right));',
      '    return x;',
      '}',
      '',
      'Node* rotateLeft(Node* x) {',
      '    Node* y = x->right; Node* T2 = y->left;',
      '    y->left = x; x->right = T2;',
      '    x->height = 1 + std::max(height(x->left), height(x->right));',
      '    y->height = 1 + std::max(height(y->left), height(y->right));',
      '    return y;',
      '}',
      '',
      'Node* insertAVL(Node* node, int key) {',
      '    if (!node) return new Node(key);',
      '    if (key < node->key) node->left = insertAVL(node->left, key);',
      '    else if (key > node->key) node->right = insertAVL(node->right, key);',
      '    else return node;',
      '    node->height = 1 + std::max(height(node->left), height(node->right));',
      '    int b = getBalance(node);',
      '    if (b > 1 && key < node->left->key) return rotateRight(node);',
      '    if (b < -1 && key > node->right->key) return rotateLeft(node);',
      '    if (b > 1 && key > node->left->key) { node->left = rotateLeft(node->left); return rotateRight(node); }',
      '    if (b < -1 && key < node->right->key) { node->right = rotateRight(node->right); return rotateLeft(node); }',
      '    return node;',
      '}',
    ],
    java: [
      'class Node {',
      '    int key, height = 1;',
      '    Node left, right;',
      '    Node(int k) { key = k; }',
      '}',
      '',
      'public class AVLTree {',
      '    int height(Node n) { return n == null ? 0 : n.height; }',
      '    int getBalance(Node n) { return n == null ? 0 : height(n.left) - height(n.right); }',
      '',
      '    Node rotateRight(Node y) {',
      '        Node x = y.left, T2 = x.right;',
      '        x.right = y; y.left = T2;',
      '        y.height = 1 + Math.max(height(y.left), height(y.right));',
      '        x.height = 1 + Math.max(height(x.left), height(x.right));',
      '        return x;',
      '    }',
      '',
      '    Node rotateLeft(Node x) {',
      '        Node y = x.right, T2 = y.left;',
      '        y.left = x; x.right = T2;',
      '        x.height = 1 + Math.max(height(x.left), height(x.right));',
      '        y.height = 1 + Math.max(height(y.left), height(y.right));',
      '        return y;',
      '    }',
      '',
      '    Node insert(Node node, int key) {',
      '        if (node == null) return new Node(key);',
      '        if (key < node.key) node.left = insert(node.left, key);',
      '        else if (key > node.key) node.right = insert(node.right, key);',
      '        else return node;',
      '        node.height = 1 + Math.max(height(node.left), height(node.right));',
      '        int b = getBalance(node);',
      '        if (b > 1 && key < node.left.key) return rotateRight(node);',
      '        if (b < -1 && key > node.right.key) return rotateLeft(node);',
      '        if (b > 1 && key > node.left.key) { node.left = rotateLeft(node.left); return rotateRight(node); }',
      '        if (b < -1 && key < node.right.key) { node.right = rotateRight(node.right); return rotateLeft(node); }',
      '        return node;',
      '    }',
      '}',
    ],
    javascript: [
      'class Node {',
      '  constructor(key) {',
      '    this.key = key;',
      '    this.left = null;',
      '    this.right = null;',
      '    this.height = 1;',
      '  }',
      '}',
      '',
      'const height = (n) => n ? n.height : 0;',
      'const getBalance = (n) => n ? height(n.left) - height(n.right) : 0;',
      '',
      'function rotateRight(y) {',
      '  const x = y.left, T2 = x.right;',
      '  x.right = y; y.left = T2;',
      '  y.height = 1 + Math.max(height(y.left), height(y.right));',
      '  x.height = 1 + Math.max(height(x.left), height(x.right));',
      '  return x;',
      '}',
      '',
      'function rotateLeft(x) {',
      '  const y = x.right, T2 = y.left;',
      '  y.left = x; x.right = T2;',
      '  x.height = 1 + Math.max(height(x.left), height(x.right));',
      '  y.height = 1 + Math.max(height(y.left), height(y.right));',
      '  return y;',
      '}',
      '',
      'function insertAVL(node, key) {',
      '  if (!node) return new Node(key);',
      '  if (key < node.key) node.left = insertAVL(node.left, key);',
      '  else if (key > node.key) node.right = insertAVL(node.right, key);',
      '  else return node;',
      '  node.height = 1 + Math.max(height(node.left), height(node.right));',
      '  const b = getBalance(node);',
      '  if (b > 1 && key < node.left.key) return rotateRight(node);',
      '  if (b < -1 && key > node.right.key) return rotateLeft(node);',
      '  if (b > 1 && key > node.left.key) { node.left = rotateLeft(node.left); return rotateRight(node); }',
      '  if (b < -1 && key < node.right.key) { node.right = rotateRight(node.right); return rotateLeft(node); }',
      '  return node;',
      '}',
    ],
  },

  'heap': {
    pseudocode: [
      '// Max-Heap Insert',
      'function insert(heap, value):',
      '  heap.append(value)',
      '  i ← heap.size - 1',
      '  while i > 0:',
      '    parent ← ⌊(i - 1) / 2⌋',
      '    if heap[i] > heap[parent]:',
      '      swap(heap[i], heap[parent])',
      '      i ← parent',
      '    else: break',
      '',
      '// Extract Max',
      'function extractMax(heap):',
      '  max ← heap[0]',
      '  heap[0] ← heap[last]',
      '  heap.removeLast()',
      '  heapifyDown(heap, 0)',
      '  return max',
    ],
    python: [
      'class MaxHeap:',
      '    def __init__(self):',
      '        self.heap = []',
      '',
      '    def insert(self, val):',
      '        self.heap.append(val)',
      '        self._shift_up(len(self.heap) - 1)',
      '',
      '    def extract_max(self):',
      '        if not self.heap: return None',
      '        max_val = self.heap[0]',
      '        self.heap[0] = self.heap[-1]',
      '        self.heap.pop()',
      '        self._shift_down(0)',
      '        return max_val',
      '',
      '    def _shift_up(self, i):',
      '        parent = (i - 1) // 2',
      '        while i > 0 and self.heap[i] > self.heap[parent]:',
      '            self.heap[i], self.heap[parent] = self.heap[parent], self.heap[i]',
      '            i = parent; parent = (i - 1) // 2',
      '',
      '    def _shift_down(self, i):',
      '        n = len(self.heap)',
      '        while 2 * i + 1 < n:',
      '            largest = 2 * i + 1',
      '            right = 2 * i + 2',
      '            if right < n and self.heap[right] > self.heap[largest]: largest = right',
      '            if self.heap[i] >= self.heap[largest]: break',
      '            self.heap[i], self.heap[largest] = self.heap[largest], self.heap[i]',
      '            i = largest',
    ],
    cpp: [
      '#include <vector>',
      '#include <algorithm>',
      '#include <stdexcept>',
      '',
      'class MaxHeap {',
      '    std::vector<int> heap;',
      '    void shiftUp(int i) {',
      '        int p = (i - 1) / 2;',
      '        while (i > 0 && heap[i] > heap[p]) {',
      '            std::swap(heap[i], heap[p]);',
      '            i = p; p = (i - 1) / 2;',
      '        }',
      '    }',
      '    void shiftDown(int i) {',
      '        int n = heap.size();',
      '        while (2 * i + 1 < n) {',
      '            int largest = 2 * i + 1, r = 2 * i + 2;',
      '            if (r < n && heap[r] > heap[largest]) largest = r;',
      '            if (heap[i] >= heap[largest]) break;',
      '            std::swap(heap[i], heap[largest]);',
      '            i = largest;',
      '        }',
      '    }',
      'public:',
      '    void insert(int val) { heap.push_back(val); shiftUp(heap.size() - 1); }',
      '    int extractMax() {',
      '        if (heap.empty()) throw std::runtime_error("Heap empty");',
      '        int m = heap[0]; heap[0] = heap.back(); heap.pop_back();',
      '        if (!heap.empty()) shiftDown(0);',
      '        return m;',
      '    }',
      '};',
    ],
    java: [
      'import java.util.ArrayList;',
      'import java.util.Collections;',
      '',
      'public class MaxHeap {',
      '    private ArrayList<Integer> heap = new ArrayList<>();',
      '',
      '    public void insert(int val) {',
      '        heap.add(val);',
      '        shiftUp(heap.size() - 1);',
      '    }',
      '',
      '    public int extractMax() {',
      '        int max = heap.get(0);',
      '        heap.set(0, heap.get(heap.size() - 1));',
      '        heap.remove(heap.size() - 1);',
      '        if (!heap.isEmpty()) shiftDown(0);',
      '        return max;',
      '    }',
      '',
      '    private void shiftUp(int i) {',
      '        int p = (i - 1) / 2;',
      '        while (i > 0 && heap.get(i) > heap.get(p)) {',
      '            Collections.swap(heap, i, p);',
      '            i = p; p = (i - 1) / 2;',
      '        }',
      '    }',
      '',
      '    private void shiftDown(int i) {',
      '        int n = heap.size();',
      '        while (2 * i + 1 < n) {',
      '            int largest = 2 * i + 1, r = 2 * i + 2;',
      '            if (r < n && heap.get(r) > heap.get(largest)) largest = r;',
      '            if (heap.get(i) >= heap.get(largest)) break;',
      '            Collections.swap(heap, i, largest);',
      '            i = largest;',
      '        }',
      '    }',
      '}',
    ],
    javascript: [
      'class MaxHeap {',
      '  constructor() { this.heap = []; }',
      '  insert(val) {',
      '    this.heap.push(val);',
      '    this._shiftUp(this.heap.length - 1);',
      '  }',
      '  extractMax() {',
      '    if (this.heap.length === 0) return null;',
      '    const max = this.heap[0];',
      '    const last = this.heap.pop();',
      '    if (this.heap.length > 0) {',
      '      this.heap[0] = last;',
      '      this._shiftDown(0);',
      '    }',
      '    return max;',
      '  }',
      '  _shiftUp(i) {',
      '    let p = Math.floor((i - 1) / 2);',
      '    while (i > 0 && this.heap[i] > this.heap[p]) {',
      '      [this.heap[i], this.heap[p]] = [this.heap[p], this.heap[i]];',
      '      i = p; p = Math.floor((i - 1) / 2);',
      '    }',
      '  }',
      '  _shiftDown(i) {',
      '    const n = this.heap.length;',
      '    while (2 * i + 1 < n) {',
      '      let largest = 2 * i + 1, r = 2 * i + 2;',
      '      if (r < n && this.heap[r] > this.heap[largest]) largest = r;',
      '      if (this.heap[i] >= this.heap[largest]) break;',
      '      [this.heap[i], this.heap[largest]] = [this.heap[largest], this.heap[i]];',
      '      i = largest;',
      '    }',
      '  }',
      '}',
    ],
  },

  'trie': {
    pseudocode: [
      '// Trie Insert',
      'function insert(root, word):',
      '  node ← root',
      '  for each char in word:',
      '    if char not in node.children:',
      '      node.children[char] ← newNode(char)',
      '    node ← node.children[char]',
      '  node.isEndOfWord ← true',
      '',
      '// Trie Search',
      'function search(root, word):',
      '  node ← root',
      '  for each char in word:',
      '    if char not in node.children: return false',
      '    node ← node.children[char]',
      '  return node.isEndOfWord',
    ],
    python: [
      'class TrieNode:',
      '    def __init__(self):',
      '        self.children = {}',
      '        self.is_end_of_word = False',
      '',
      'class Trie:',
      '    def __init__(self):',
      '        self.root = TrieNode()',
      '',
      '    def insert(self, word: str):',
      '        node = self.root',
      '        for char in word:',
      '            if char not in node.children:',
      '                node.children[char] = TrieNode()',
      '            node = node.children[char]',
      '        node.is_end_of_word = True',
      '',
      '    def search(self, word: str) -> bool:',
      '        node = self.root',
      '        for char in word:',
      '            if char not in node.children:',
      '                return False',
      '            node = node.children[char]',
      '        return node.is_end_of_word',
    ],
    cpp: [
      '#include <unordered_map>',
      '#include <string>',
      '',
      'struct TrieNode {',
      '    std::unordered_map<char, TrieNode*> children;',
      '    bool isEndOfWord = false;',
      '};',
      '',
      'class Trie {',
      '    TrieNode* root;',
      'public:',
      '    Trie() { root = new TrieNode(); }',
      '    void insert(std::string word) {',
      '        TrieNode* node = root;',
      '        for (char c : word) {',
      '            if (!node->children.count(c)) node->children[c] = new TrieNode();',
      '            node = node->children[c];',
      '        }',
      '        node->isEndOfWord = true;',
      '    }',
      '    bool search(std::string word) {',
      '        TrieNode* node = root;',
      '        for (char c : word) {',
      '            if (!node->children.count(c)) return false;',
      '            node = node->children[c];',
      '        }',
      '        return node->isEndOfWord;',
      '    }',
      '};',
    ],
    java: [
      'import java.util.HashMap;',
      'import java.util.Map;',
      '',
      'class TrieNode {',
      '    Map<Character, TrieNode> children = new HashMap<>();',
      '    boolean isEndOfWord = false;',
      '}',
      '',
      'public class Trie {',
      '    private TrieNode root = new TrieNode();',
      '',
      '    public void insert(String word) {',
      '        TrieNode node = root;',
      '        for (char c : word.toCharArray()) {',
      '            node.children.putIfAbsent(c, new TrieNode());',
      '            node = node.children.get(c);',
      '        }',
      '        node.isEndOfWord = true;',
      '    }',
      '',
      '    public boolean search(String word) {',
      '        TrieNode node = root;',
      '        for (char c : word.toCharArray()) {',
      '            if (!node.children.containsKey(c)) return false;',
      '            node = node.children.get(c);',
      '        }',
      '        return node.isEndOfWord;',
      '    }',
      '}',
    ],
    javascript: [
      'class TrieNode {',
      '  constructor() {',
      '    this.children = {};',
      '    this.isEndOfWord = false;',
      '  }',
      '}',
      '',
      'class Trie {',
      '  constructor() { this.root = new TrieNode(); }',
      '  insert(word) {',
      '    let node = this.root;',
      '    for (const char of word) {',
      '      if (!node.children[char]) node.children[char] = new TrieNode();',
      '      node = node.children[char];',
      '    }',
      '    node.isEndOfWord = true;',
      '  }',
      '  search(word) {',
      '    let node = this.root;',
      '    for (const char of word) {',
      '      if (!node.children[char]) return false;',
      '      node = node.children[char];',
      '    }',
      '    return node.isEndOfWord;',
      '  }',
      '}',
    ],
  },

  // ════════════════════════════════════════════════════
  //  GRAPH ALGORITHMS
  // ════════════════════════════════════════════════════
  'bfs': {
    pseudocode: [
      'procedure BFS(G, start_node)',
      '  Q ← queue containing start_node',
      '  mark start_node as visited',
      '  while Q is not empty do',
      '    curr ← Q.dequeue()',
      '    for each neighbor of curr do',
      '      if neighbor is not visited then',
      '        mark neighbor as visited',
      '        Q.enqueue(neighbor)',
      '      end if',
      '    end for',
      '  end while',
      'end procedure',
    ],
    python: [
      'from collections import deque',
      '',
      'def bfs(graph, start):',
      '    visited = set([start])',
      '    queue = deque([start])',
      '    result = []',
      '    while queue:',
      '        curr = queue.popleft()',
      '        result.append(curr)',
      '        for neighbor in graph.get(curr, []):',
      '            if neighbor not in visited:',
      '                visited.add(neighbor)',
      '                queue.append(neighbor)',
      '    return result',
    ],
    cpp: [
      '#include <vector>',
      '#include <queue>',
      '#include <unordered_set>',
      '#include <unordered_map>',
      '',
      'std::vector<int> bfs(const std::unordered_map<int, std::vector<int>>& graph, int start) {',
      '    std::vector<int> result;',
      '    std::unordered_set<int> visited;',
      '    std::queue<int> q;',
      '    visited.insert(start);',
      '    q.push(start);',
      '    while (!q.empty()) {',
      '        int curr = q.front(); q.pop();',
      '        result.push_back(curr);',
      '        if (graph.count(curr)) {',
      '            for (int neighbor : graph.at(curr)) {',
      '                if (!visited.count(neighbor)) {',
      '                    visited.insert(neighbor);',
      '                    q.push(neighbor);',
      '                }',
      '            }',
      '        }',
      '    }',
      '    return result;',
      '}',
    ],
    java: [
      'import java.util.*;',
      '',
      'public class GraphBFS {',
      '    public static List<Integer> bfs(Map<Integer, List<Integer>> graph, int start) {',
      '        List<Integer> result = new ArrayList<>();',
      '        Set<Integer> visited = new HashSet<>();',
      '        Queue<Integer> queue = new LinkedList<>();',
      '        visited.add(start);',
      '        queue.add(start);',
      '        while (!queue.isEmpty()) {',
      '            int curr = queue.poll();',
      '            result.add(curr);',
      '            for (int neighbor : graph.getOrDefault(curr, Collections.emptyList())) {',
      '                if (!visited.contains(neighbor)) {',
      '                    visited.add(neighbor);',
      '                    queue.add(neighbor);',
      '                }',
      '            }',
      '        }',
      '        return result;',
      '    }',
      '}',
    ],
    javascript: [
      'function bfs(graph, start) {',
      '  const visited = new Set([start]);',
      '  const queue = [start];',
      '  const result = [];',
      '  while (queue.length > 0) {',
      '    const curr = queue.shift();',
      '    result.push(curr);',
      '    const neighbors = graph[curr] || [];',
      '    for (const neighbor of neighbors) {',
      '      if (!visited.has(neighbor)) {',
      '        visited.add(neighbor);',
      '        queue.push(neighbor);',
      '      }',
      '    }',
      '  }',
      '  return result;',
      '}',
    ],
  },

  'dfs': {
    pseudocode: [
      'procedure DFS(G, start_node)',
      '  S ← stack containing start_node',
      '  while S is not empty do',
      '    curr ← S.pop()',
      '    if curr is not visited then',
      '      mark curr as visited',
      '      for each neighbor of curr do',
      '        S.push(neighbor)',
      '      end for',
      '    end if',
      '  end while',
      'end procedure',
    ],
    python: [
      'def dfs_recursive(graph, curr, visited=None, result=None):',
      '    if visited is None: visited = set()',
      '    if result is None: result = []',
      '    visited.add(curr)',
      '    result.append(curr)',
      '    for neighbor in graph.get(curr, []):',
      '        if neighbor not in visited:',
      '            dfs_recursive(graph, neighbor, visited, result)',
      '    return result',
    ],
    cpp: [
      '#include <vector>',
      '#include <unordered_set>',
      '#include <unordered_map>',
      '',
      'void dfsHelper(const std::unordered_map<int, std::vector<int>>& graph, int curr, std::unordered_set<int>& visited, std::vector<int>& result) {',
      '    visited.insert(curr);',
      '    result.push_back(curr);',
      '    if (graph.count(curr)) {',
      '        for (int neighbor : graph.at(curr)) {',
      '            if (!visited.count(neighbor)) {',
      '                dfsHelper(graph, neighbor, visited, result);',
      '            }',
      '        }',
      '    }',
      '}',
      '',
      'std::vector<int> dfs(const std::unordered_map<int, std::vector<int>>& graph, int start) {',
      '    std::vector<int> result;',
      '    std::unordered_set<int> visited;',
      '    dfsHelper(graph, start, visited, result);',
      '    return result;',
      '}',
    ],
    java: [
      'import java.util.*;',
      '',
      'public class GraphDFS {',
      '    public static List<Integer> dfs(Map<Integer, List<Integer>> graph, int start) {',
      '        List<Integer> result = new ArrayList<>();',
      '        Set<Integer> visited = new HashSet<>();',
      '        dfsHelper(graph, start, visited, result);',
      '        return result;',
      '    }',
      '',
      '    private static void dfsHelper(Map<Integer, List<Integer>> graph, int curr, Set<Integer> visited, List<Integer> result) {',
      '        visited.add(curr);',
      '        result.add(curr);',
      '        for (int neighbor : graph.getOrDefault(curr, Collections.emptyList())) {',
      '            if (!visited.contains(neighbor)) {',
      '                dfsHelper(graph, neighbor, visited, result);',
      '            }',
      '        }',
      '    }',
      '}',
    ],
    javascript: [
      'function dfs(graph, start) {',
      '  const visited = new Set();',
      '  const result = [];',
      '  function traverse(curr) {',
      '    visited.add(curr);',
      '    result.push(curr);',
      '    const neighbors = graph[curr] || [];',
      '    for (const neighbor of neighbors) {',
      '      if (!visited.has(neighbor)) traverse(neighbor);',
      '    }',
      '  }',
      '  traverse(start);',
      '  return result;',
      '}',
    ],
  },

  'dijkstra': {
    pseudocode: [
      'procedure Dijkstra(G, start)',
      '  dist[start] ← 0, all others ← ∞',
      '  PQ ← priority queue with (0, start)',
      '  while PQ is not empty do',
      '    (d, u) ← PQ.extractMin()',
      '    if d > dist[u] then continue',
      '    for each (v, weight) of neighbors(u) do',
      '      if dist[u] + weight < dist[v] then',
      '        dist[v] ← dist[u] + weight',
      '        PQ.insert(dist[v], v)',
      '      end if',
      '    end for',
      '  end while',
      'end procedure',
    ],
    python: [
      'import heapq',
      '',
      'def dijkstra(graph, start):',
      '    dist = {node: float("inf") for node in graph}',
      '    dist[start] = 0',
      '    pq = [(0, start)]',
      '    while pq:',
      '        d, u = heapq.heappop(pq)',
      '        if d > dist[u]: continue',
      '        for v, weight in graph[u].items():',
      '            if dist[u] + weight < dist[v]:',
      '                dist[v] = dist[u] + weight',
      '                heapq.heappush(pq, (dist[v], v))',
      '    return dist',
    ],
    cpp: [
      '#include <vector>',
      '#include <queue>',
      '#include <unordered_map>',
      '#include <limits>',
      '',
      'std::unordered_map<int, int> dijkstra(const std::unordered_map<int, std::vector<std::pair<int, int>>>& graph, int start) {',
      '    std::unordered_map<int, int> dist;',
      '    for (const auto& pair : graph) dist[pair.first] = std::numeric_limits<int>::max();',
      '    dist[start] = 0;',
      '    using PII = std::pair<int, int>; // (dist, node)',
      '    std::priority_queue<PII, std::vector<PII>, std::greater<PII>> pq;',
      '    pq.push({0, start});',
      '    while (!pq.empty()) {',
      '        auto [d, u] = pq.top(); pq.pop();',
      '        if (d > dist[u]) continue;',
      '        if (graph.count(u)) {',
      '            for (const auto& [v, weight] : graph.at(u)) {',
      '                if (dist[u] + weight < dist[v]) {',
      '                    dist[v] = dist[u] + weight;',
      '                    pq.push({dist[v], v});',
      '                }',
      '            }',
      '        }',
      '    }',
      '    return dist;',
      '}',
    ],
    java: [
      'import java.util.*;',
      '',
      'public class Dijkstra {',
      '    static class Edge { int to, weight; Edge(int t, int w) { to = t; weight = w; } }',
      '    public static Map<Integer, Integer> dijkstra(Map<Integer, List<Edge>> graph, int start) {',
      '        Map<Integer, Integer> dist = new HashMap<>();',
      '        for (int node : graph.keySet()) dist.put(node, Integer.MAX_VALUE);',
      '        dist.put(start, 0);',
      '        PriorityQueue<int[]> pq = new PriorityQueue<>(Comparator.comparingInt(a -> a[0]));',
      '        pq.add(new int[]{0, start});',
      '        while (!pq.isEmpty()) {',
      '            int[] curr = pq.poll();',
      '            int d = curr[0], u = curr[1];',
      '            if (d > dist.get(u)) continue;',
      '            for (Edge edge : graph.getOrDefault(u, Collections.emptyList())) {',
      '                if (dist.get(u) + edge.weight < dist.get(edge.to)) {',
      '                    dist.put(edge.to, dist.get(u) + edge.weight);',
      '                    pq.add(new int[]{dist.get(edge.to), edge.to});',
      '                }',
      '            }',
      '        }',
      '        return dist;',
      '    }',
      '}',
    ],
    javascript: [
      'function dijkstra(graph, start) {',
      '  const dist = {};',
      '  for (const node in graph) dist[node] = Infinity;',
      '  dist[start] = 0;',
      '  const visited = new Set();',
      '  while (true) {',
      '    let u = null, minD = Infinity;',
      '    for (const node in dist) {',
      '      if (!visited.has(node) && dist[node] < minD) {',
      '        minD = dist[node]; u = node;',
      '      }',
      '    }',
      '    if (u === null) break;',
      '    visited.add(u);',
      '    for (const [v, weight] of Object.entries(graph[u] || {})) {',
      '      if (dist[u] + weight < dist[v]) {',
      '        dist[v] = dist[u] + weight;',
      '      }',
      '    }',
      '  }',
      '  return dist;',
      '}',
    ],
  },

  'prims-mst': {
    pseudocode: [
      'procedure PrimMST(G, start)',
      '  MST ← empty list',
      '  visited[start] ← true',
      '  PQ ← all edges connected to start',
      '  while PQ is not empty and |MST| < V-1 do',
      '    (u, v, weight) ← PQ.extractMin()',
      '    if v is visited then continue',
      '    add (u, v, weight) to MST',
      '    visited[v] ← true',
      '    for each (w, weight) of neighbors(v) do',
      '      if w is not visited then PQ.insert(v, w, weight)',
      '    end for',
      '  end while',
      'end procedure',
    ],
    python: [
      'import heapq',
      '',
      'def prim_mst(graph, start):',
      '    mst = []',
      '    visited = set([start])',
      '    pq = [(weight, start, v) for v, weight in graph[start].items()]',
      '    heapq.heapify(pq)',
      '    total_cost = 0',
      '    while pq:',
      '        weight, u, v = heapq.heappop(pq)',
      '        if v in visited: continue',
      '        visited.add(v)',
      '        mst.append((u, v, weight))',
      '        total_cost += weight',
      '        for next_node, w in graph[v].items():',
      '            if next_node not in visited:',
      '                heapq.heappush(pq, (w, v, next_node))',
      '    return mst, total_cost',
    ],
    cpp: [
      '#include <vector>',
      '#include <queue>',
      '#include <unordered_set>',
      '#include <unordered_map>',
      '#include <tuple>',
      '',
      'using EdgeTuple = std::tuple<int, int, int>; // (weight, u, v)',
      '',
      'std::vector<EdgeTuple> primMST(const std::unordered_map<int, std::vector<std::pair<int, int>>>& graph, int start) {',
      '    std::vector<EdgeTuple> mst;',
      '    std::unordered_set<int> visited;',
      '    visited.insert(start);',
      '    std::priority_queue<EdgeTuple, std::vector<EdgeTuple>, std::greater<EdgeTuple>> pq;',
      '    for (const auto& [v, w] : graph.at(start)) pq.push({w, start, v});',
      '    while (!pq.empty()) {',
      '        auto [w, u, v] = pq.top(); pq.pop();',
      '        if (visited.count(v)) continue;',
      '        visited.insert(v);',
      '        mst.push_back({w, u, v});',
      '        for (const auto& [next, weight] : graph.at(v)) {',
      '            if (!visited.count(next)) pq.push({weight, v, next});',
      '        }',
      '    }',
      '    return mst;',
      '}',
    ],
    java: [
      'import java.util.*;',
      '',
      'public class PrimMST {',
      '    public static List<int[]> prim(Map<Integer, List<int[]>> graph, int start) {',
      '        List<int[]> mst = new ArrayList<>();',
      '        Set<Integer> visited = new HashSet<>();',
      '        visited.add(start);',
      '        PriorityQueue<int[]> pq = new PriorityQueue<>(Comparator.comparingInt(a -> a[0]));',
      '        for (int[] edge : graph.getOrDefault(start, Collections.emptyList()))',
      '            pq.add(new int[]{edge[1], start, edge[0]}); // weight, u, v',
      '        while (!pq.isEmpty()) {',
      '            int[] curr = pq.poll();',
      '            int w = curr[0], u = curr[1], v = curr[2];',
      '            if (visited.contains(v)) continue;',
      '            visited.add(v);',
      '            mst.add(new int[]{u, v, w});',
      '            for (int[] edge : graph.getOrDefault(v, Collections.emptyList()))',
      '                if (!visited.contains(edge[0])) pq.add(new int[]{edge[1], v, edge[0]});',
      '        }',
      '        return mst;',
      '    }',
      '}',
    ],
    javascript: [
      'function primMST(graph, start) {',
      '  const mst = [];',
      '  const visited = new Set([start]);',
      '  const edges = [];',
      '  for (const [v, w] of Object.entries(graph[start] || {})) edges.push({ w, u: start, v });',
      '  while (edges.length > 0) {',
      '    edges.sort((a, b) => a.w - b.w);',
      '    const { w, u, v } = edges.shift();',
      '    if (visited.has(v)) continue;',
      '    visited.add(v);',
      '    mst.push({ u, v, w });',
      '    for (const [next, weight] of Object.entries(graph[v] || {})) {',
      '      if (!visited.has(next)) edges.push({ w: weight, u: v, v: next });',
      '    }',
      '  }',
      '  return mst;',
      '}',
    ],
  },

  'kruskals-mst': {
    pseudocode: [
      'procedure KruskalMST(edges, V)',
      '  sort edges by weight ascending',
      '  DSU ← DisjointSet(V)',
      '  MST ← empty list',
      '  for each edge (u, v, weight) in sorted edges do',
      '    if DSU.find(u) ≠ DSU.find(v) then',
      '      DSU.union(u, v)',
      '      add edge to MST',
      '    end if',
      '  end for',
      'end procedure',
    ],
    python: [
      'class DSU:',
      '    def __init__(self, n):',
      '        self.parent = list(range(n))',
      '    def find(self, i):',
      '        if self.parent[i] == i: return i',
      '        self.parent[i] = self.find(self.parent[i])',
      '        return self.parent[i]',
      '    def union(self, i, j):',
      '        root_i, root_j = self.find(i), self.find(j)',
      '        if root_i != root_j: self.parent[root_i] = root_j; return True',
      '        return False',
      '',
      'def kruskal_mst(edges, n):',
      '    edges.sort(key=lambda e: e[2])  # (u, v, weight)',
      '    dsu = DSU(n)',
      '    mst = []',
      '    for u, v, w in edges:',
      '        if dsu.union(u, v): mst.append((u, v, w))',
      '    return mst',
    ],
    cpp: [
      '#include <vector>',
      '#include <algorithm>',
      '#include <numeric>',
      '',
      'struct DSU {',
      '    std::vector<int> parent;',
      '    DSU(int n) { parent.resize(n); std::iota(parent.begin(), parent.end(), 0); }',
      '    int find(int i) { return parent[i] == i ? i : parent[i] = find(parent[i]); }',
      '    bool unite(int i, int j) {',
      '        int rootI = find(i), rootJ = find(j);',
      '        if (rootI != rootJ) { parent[rootI] = rootJ; return true; }',
      '        return false;',
      '    }',
      '};',
      '',
      'struct Edge { int u, v, w; };',
      'std::vector<Edge> kruskalMST(std::vector<Edge>& edges, int n) {',
      '    std::sort(edges.begin(), edges.end(), [](const Edge& a, const Edge& b){ return a.w < b.w; });',
      '    DSU dsu(n); std::vector<Edge> mst;',
      '    for (const auto& e : edges) if (dsu.unite(e.u, e.v)) mst.push_back(e);',
      '    return mst;',
      '}',
    ],
    java: [
      'import java.util.*;',
      '',
      'public class KruskalMST {',
      '    static class DSU {',
      '        int[] parent;',
      '        DSU(int n) { parent = new int[n]; for (int i = 0; i < n; i++) parent[i] = i; }',
      '        int find(int i) { return parent[i] == i ? i : (parent[i] = find(parent[i])); }',
      '        boolean union(int i, int j) {',
      '            int rI = find(i), rJ = find(j);',
      '            if (rI != rJ) { parent[rI] = rJ; return true; }',
      '            return false;',
      '        }',
      '    }',
      '    public static List<int[]> kruskal(int[][] edges, int n) {',
      '        Arrays.sort(edges, Comparator.comparingInt(a -> a[2]));',
      '        DSU dsu = new DSU(n); List<int[]> mst = new ArrayList<>();',
      '        for (int[] e : edges) if (dsu.union(e[0], e[1])) mst.add(e);',
      '        return mst;',
      '    }',
      '}',
    ],
    javascript: [
      'class DSU {',
      '  constructor(n) { this.parent = Array.from({ length: n }, (_, i) => i); }',
      '  find(i) { return this.parent[i] === i ? i : (this.parent[i] = this.find(this.parent[i])); }',
      '  union(i, j) {',
      '    const rI = this.find(i), rJ = this.find(j);',
      '    if (rI !== rJ) { this.parent[rI] = rJ; return true; }',
      '    return false;',
      '  }',
      '}',
      '',
      'function kruskalMST(edges, n) {',
      '  edges.sort((a, b) => a[2] - b[2]);',
      '  const dsu = new DSU(n);',
      '  const mst = [];',
      '  for (const [u, v, w] of edges) if (dsu.union(u, v)) mst.push([u, v, w]);',
      '  return mst;',
      '}',
    ],
  },

  'floyd-warshall': {
    pseudocode: [
      'procedure FloydWarshall(G, V)',
      '  dist ← V × V matrix initialized to ∞',
      '  for each vertex v do dist[v][v] ← 0',
      '  for each edge (u, v, w) do dist[u][v] ← w',
      '  for k ← 0 to V-1 do',
      '    for i ← 0 to V-1 do',
      '      for j ← 0 to V-1 do',
      '        if dist[i][k] + dist[k][j] < dist[i][j] then',
      '          dist[i][j] ← dist[i][k] + dist[k][j]',
      '        end if',
      '      end for',
      '    end for',
      '  end for',
      'end procedure',
    ],
    python: [
      'def floyd_warshall(graph_matrix, n):',
      '    dist = [row[:] for row in graph_matrix]',
      '    for k in range(n):',
      '        for i in range(n):',
      '            for j in range(n):',
      '                if dist[i][k] + dist[k][j] < dist[i][j]:',
      '                    dist[i][j] = dist[i][k] + dist[k][j]',
      '    return dist',
    ],
    cpp: [
      '#include <vector>',
      '#include <algorithm>',
      '',
      'void floydWarshall(std::vector<std::vector<int>>& dist, int n) {',
      '    for (int k = 0; k < n; k++) {',
      '        for (int i = 0; i < n; i++) {',
      '            for (int j = 0; j < n; j++) {',
      '                if (dist[i][k] != 1e9 && dist[k][j] != 1e9 && dist[i][k] + dist[k][j] < dist[i][j]) {',
      '                    dist[i][j] = dist[i][k] + dist[k][j];',
      '                }',
      '            }',
      '        }',
      '    }',
      '}',
    ],
    java: [
      'public class FloydWarshall {',
      '    public static void solve(int[][] dist, int n) {',
      '        for (int k = 0; k < n; k++) {',
      '            for (int i = 0; i < n; i++) {',
      '                for (int j = 0; j < n; j++) {',
      '                    if (dist[i][k] != 1e9 && dist[k][j] != 1e9 && dist[i][k] + dist[k][j] < dist[i][j]) {',
      '                        dist[i][j] = dist[i][k] + dist[k][j];',
      '                    }',
      '                }',
      '            }',
      '        }',
      '    }',
      '}',
    ],
    javascript: [
      'function floydWarshall(matrix, n) {',
      '  const dist = matrix.map(row => [...row]);',
      '  for (let k = 0; k < n; k++) {',
      '    for (let i = 0; i < n; i++) {',
      '      for (let j = 0; j < n; j++) {',
      '        if (dist[i][k] + dist[k][j] < dist[i][j]) {',
      '          dist[i][j] = dist[i][k] + dist[k][j];',
      '        }',
      '      }',
      '    }',
      '  }',
      '  return dist;',
      '}',
    ],
  },

  'topological-sort': {
    pseudocode: [
      'procedure TopologicalSort(G, V)',
      '  inDegree ← array of in-degrees for each vertex',
      '  Q ← queue of vertices with inDegree = 0',
      '  topoOrder ← empty list',
      '  while Q is not empty do',
      '    u ← Q.dequeue()',
      '    topoOrder.append(u)',
      '    for each neighbor v of u do',
      '      inDegree[v] ← inDegree[v] - 1',
      '      if inDegree[v] = 0 then Q.enqueue(v)',
      '    end for',
      '  end while',
      '  return topoOrder',
      'end procedure',
    ],
    python: [
      'from collections import deque',
      '',
      'def topological_sort(graph, n):',
      '    in_degree = {i: 0 for i in range(n)}',
      '    for u in graph:',
      '        for v in graph[u]: in_degree[v] += 1',
      '    q = deque([u for u in in_degree if in_degree[u] == 0])',
      '    result = []',
      '    while q:',
      '        u = q.popleft()',
      '        result.append(u)',
      '        for v in graph.get(u, []):',
      '            in_degree[v] -= 1',
      '            if in_degree[v] == 0: q.append(v)',
      '    return result if len(result) == n else [] # Cycle check',
    ],
    cpp: [
      '#include <vector>',
      '#include <queue>',
      '#include <unordered_map>',
      '',
      'std::vector<int> topologicalSort(const std::unordered_map<int, std::vector<int>>& graph, int n) {',
      '    std::vector<int> inDegree(n, 0);',
      '    for (const auto& [u, neighbors] : graph)',
      '        for (int v : neighbors) inDegree[v]++;',
      '    std::queue<int> q;',
      '    for (int i = 0; i < n; i++) if (inDegree[i] == 0) q.push(i);',
      '    std::vector<int> result;',
      '    while (!q.empty()) {',
      '        int u = q.front(); q.pop();',
      '        result.push_back(u);',
      '        if (graph.count(u)) {',
      '            for (int v : graph.at(u)) {',
      '                if (--inDegree[v] == 0) q.push(v);',
      '            }',
      '        }',
      '    }',
      '    return result;',
      '}',
    ],
    java: [
      'import java.util.*;',
      '',
      'public class TopologicalSort {',
      '    public static List<Integer> sort(Map<Integer, List<Integer>> graph, int n) {',
      '        int[] inDegree = new int[n];',
      '        for (List<Integer> neighbors : graph.values())',
      '            for (int v : neighbors) inDegree[v]++;',
      '        Queue<Integer> q = new LinkedList<>();',
      '        for (int i = 0; i < n; i++) if (inDegree[i] == 0) q.add(i);',
      '        List<Integer> result = new ArrayList<>();',
      '        while (!q.isEmpty()) {',
      '            int u = q.poll();',
      '            result.add(u);',
      '            for (int v : graph.getOrDefault(u, Collections.emptyList()))',
      '                if (--inDegree[v] == 0) q.add(v);',
      '        }',
      '        return result.size() == n ? result : new ArrayList<>();',
      '    }',
      '}',
    ],
    javascript: [
      'function topologicalSort(graph, n) {',
      '  const inDegree = new Array(n).fill(0);',
      '  for (const u in graph)',
      '    for (const v of graph[u]) inDegree[v]++;',
      '  const queue = [];',
      '  for (let i = 0; i < n; i++) if (inDegree[i] === 0) queue.push(i);',
      '  const result = [];',
      '  while (queue.length > 0) {',
      '    const u = queue.shift();',
      '    result.push(u);',
      '    for (const v of (graph[u] || [])) {',
      '      inDegree[v]--;',
      '      if (inDegree[v] === 0) queue.push(v);',
      '    }',
      '  }',
      '  return result;',
      '}',
    ],
  },

  // ════════════════════════════════════════════════════
  //  DYNAMIC PROGRAMMING (DP)
  // ════════════════════════════════════════════════════
  'fibonacci': {
    pseudocode: [
      'procedure FibonacciDP(n)',
      '  if n <= 1 then return n',
      '  dp ← array of size n+1',
      '  dp[0] ← 0, dp[1] ← 1',
      '  for i ← 2 to n do',
      '    dp[i] ← dp[i-1] + dp[i-2]',
      '  end for',
      '  return dp[n]',
      'end procedure',
    ],
    python: [
      'def fibonacci_dp(n):',
      '    if n <= 1: return n',
      '    dp = [0] * (n + 1)',
      '    dp[1] = 1',
      '    for i in range(2, n + 1):',
      '        dp[i] = dp[i - 1] + dp[i - 2]',
      '    return dp[n]',
    ],
    cpp: [
      '#include <vector>',
      '',
      'long long fibonacciDP(int n) {',
      '    if (n <= 1) return n;',
      '    std::vector<long long> dp(n + 1);',
      '    dp[0] = 0; dp[1] = 1;',
      '    for (int i = 2; i <= n; i++) {',
      '        dp[i] = dp[i - 1] + dp[i - 2];',
      '    }',
      '    return dp[n];',
      '}',
    ],
    java: [
      'public class FibonacciDP {',
      '    public static long solve(int n) {',
      '        if (n <= 1) return n;',
      '        long[] dp = new long[n + 1];',
      '        dp[0] = 0; dp[1] = 1;',
      '        for (int i = 2; i <= n; i++) {',
      '            dp[i] = dp[i - 1] + dp[i - 2];',
      '        }',
      '        return dp[n];',
      '    }',
      '}',
    ],
    javascript: [
      'function fibonacciDP(n) {',
      '  if (n <= 1) return n;',
      '  const dp = new Array(n + 1).fill(0);',
      '  dp[1] = 1;',
      '  for (let i = 2; i <= n; i++) {',
      '    dp[i] = dp[i - 1] + dp[i - 2];',
      '  }',
      '  return dp[n];',
      '}',
    ],
  },

  'knapsack': {
    pseudocode: [
      'procedure Knapsack01(weights, values, W, n)',
      '  dp ← (n+1) × (W+1) matrix initialized to 0',
      '  for i ← 1 to n do',
      '    for w ← 0 to W do',
      '      if weights[i-1] <= w then',
      '        dp[i][w] ← max(values[i-1] + dp[i-1][w - weights[i-1]], dp[i-1][w])',
      '      else',
      '        dp[i][w] ← dp[i-1][w]',
      '      end if',
      '    end for',
      '  end for',
      '  return dp[n][W]',
      'end procedure',
    ],
    python: [
      'def knapsack_01(weights, values, W):',
      '    n = len(weights)',
      '    dp = [[0] * (W + 1) for _ in range(n + 1)]',
      '    for i in range(1, n + 1):',
      '        for w in range(W + 1):',
      '            if weights[i - 1] <= w:',
      '                dp[i][w] = max(values[i - 1] + dp[i - 1][w - weights[i - 1]], dp[i - 1][w])',
      '            else:',
      '                dp[i][w] = dp[i - 1][w]',
      '    return dp[n][W]',
    ],
    cpp: [
      '#include <vector>',
      '#include <algorithm>',
      '',
      'int knapsack01(const std::vector<int>& weights, const std::vector<int>& values, int W) {',
      '    int n = weights.size();',
      '    std::vector<std::vector<int>> dp(n + 1, std::vector<int>(W + 1, 0));',
      '    for (int i = 1; i <= n; i++) {',
      '        for (int w = 0; w <= W; w++) {',
      '            if (weights[i - 1] <= w)',
      '                dp[i][w] = std::max(values[i - 1] + dp[i - 1][w - weights[i - 1]], dp[i - 1][w]);',
      '            else',
      '                dp[i][w] = dp[i - 1][w];',
      '        }',
      '    }',
      '    return dp[n][W];',
      '}',
    ],
    java: [
      'public class Knapsack {',
      '    public static int solve(int[] weights, int[] values, int W) {',
      '        int n = weights.length;',
      '        int[][] dp = new int[n + 1][W + 1];',
      '        for (int i = 1; i <= n; i++) {',
      '            for (int w = 0; w <= W; w++) {',
      '                if (weights[i - 1] <= w)',
      '                    dp[i][w] = Math.max(values[i - 1] + dp[i - 1][w - weights[i - 1]], dp[i - 1][w]);',
      '                else',
      '                    dp[i][w] = dp[i - 1][w];',
      '            }',
      '        }',
      '        return dp[n][W];',
      '    }',
      '}',
    ],
    javascript: [
      'function knapsack01(weights, values, W) {',
      '  const n = weights.length;',
      '  const dp = Array.from({ length: n + 1 }, () => new Array(W + 1).fill(0));',
      '  for (let i = 1; i <= n; i++) {',
      '    for (let w = 0; w <= W; w++) {',
      '      if (weights[i - 1] <= w)',
      '        dp[i][w] = Math.max(values[i - 1] + dp[i - 1][w - weights[i - 1]], dp[i - 1][w]);',
      '      else',
      '        dp[i][w] = dp[i - 1][w];',
      '    }',
      '  }',
      '  return dp[n][W];',
      '}',
    ],
  },

  'lcs': {
    pseudocode: [
      'procedure LCS(text1, text2)',
      '  m ← length(text1), n ← length(text2)',
      '  dp ← (m+1) × (n+1) matrix initialized to 0',
      '  for i ← 1 to m do',
      '    for j ← 1 to n do',
      '      if text1[i-1] = text2[j-1] then',
      '        dp[i][j] ← 1 + dp[i-1][j-1]',
      '      else',
      '        dp[i][j] ← max(dp[i-1][j], dp[i][j-1])',
      '      end if',
      '    end for',
      '  end for',
      '  return dp[m][n]',
      'end procedure',
    ],
    python: [
      'def longest_common_subsequence(text1: str, text2: str) -> int:',
      '    m, n = len(text1), len(text2)',
      '    dp = [[0] * (n + 1) for _ in range(m + 1)]',
      '    for i in range(1, m + 1):',
      '        for j in range(1, n + 1):',
      '            if text1[i - 1] == text2[j - 1]:',
      '                dp[i][j] = 1 + dp[i - 1][j - 1]',
      '            else:',
      '                dp[i][j] = max(dp[i - 1][j], dp[i][j - 1])',
      '    return dp[m][n]',
    ],
    cpp: [
      '#include <string>',
      '#include <vector>',
      '#include <algorithm>',
      '',
      'int longestCommonSubsequence(std::string text1, std::string text2) {',
      '    int m = text1.size(), n = text2.size();',
      '    std::vector<std::vector<int>> dp(m + 1, std::vector<int>(n + 1, 0));',
      '    for (int i = 1; i <= m; i++) {',
      '        for (int j = 1; j <= n; j++) {',
      '            if (text1[i - 1] == text2[j - 1]) dp[i][j] = 1 + dp[i - 1][j - 1];',
      '            else dp[i][j] = std::max(dp[i - 1][j], dp[i][j - 1]);',
      '        }',
      '    }',
      '    return dp[m][n];',
      '}',
    ],
    java: [
      'public class LCS {',
      '    public static int solve(String text1, String text2) {',
      '        int m = text1.length(), n = text2.length();',
      '        int[][] dp = new int[m + 1][n + 1];',
      '        for (int i = 1; i <= m; i++) {',
      '            for (int j = 1; j <= n; j++) {',
      '                if (text1.charAt(i - 1) == text2.charAt(j - 1))',
      '                    dp[i][j] = 1 + dp[i - 1][j - 1];',
      '                else',
      '                    dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);',
      '            }',
      '        }',
      '        return dp[m][n];',
      '    }',
      '}',
    ],
    javascript: [
      'function longestCommonSubsequence(text1, text2) {',
      '  const m = text1.length, n = text2.length;',
      '  const dp = Array.from({ length: m + 1 }, () => new Array(n + 1).fill(0));',
      '  for (let i = 1; i <= m; i++) {',
      '    for (let j = 1; j <= n; j++) {',
      '      if (text1[i - 1] === text2[j - 1]) dp[i][j] = 1 + dp[i - 1][j - 1];',
      '      else dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);',
      '    }',
      '  }',
      '  return dp[m][n];',
      '}',
    ],
  },

  'matrix-chain': {
    pseudocode: [
      'procedure MatrixChainOrder(p, n)',
      '  dp ← n × n matrix initialized to 0',
      '  for L ← 2 to n-1 do',
      '    for i ← 1 to n-L do',
      '      j ← i + L - 1',
      '      dp[i][j] ← ∞',
      '      for k ← i to j-1 do',
      '        q ← dp[i][k] + dp[k+1][j] + p[i-1]*p[k]*p[j]',
      '        if q < dp[i][j] then dp[i][j] ← q',
      '      end for',
      '    end for',
      '  end for',
      '  return dp[1][n-1]',
      'end procedure',
    ],
    python: [
      'def matrix_chain_order(p):',
      '    n = len(p)',
      '    dp = [[0] * n for _ in range(n)]',
      '    for length in range(2, n):',
      '        for i in range(1, n - length + 1):',
      '            j = i + length - 1',
      '            dp[i][j] = float("inf")',
      '            for k in range(i, j):',
      '                q = dp[i][k] + dp[k + 1][j] + p[i - 1] * p[k] * p[j]',
      '                if q < dp[i][j]:',
      '                    dp[i][j] = q',
      '    return dp[1][n - 1]',
    ],
    cpp: [
      '#include <vector>',
      '#include <algorithm>',
      '#include <limits>',
      '',
      'int matrixChainOrder(const std::vector<int>& p) {',
      '    int n = p.size();',
      '    std::vector<std::vector<int>> dp(n, std::vector<int>(n, 0));',
      '    for (int len = 2; len < n; len++) {',
      '        for (int i = 1; i < n - len + 1; i++) {',
      '            int j = i + len - 1;',
      '            dp[i][j] = std::numeric_limits<int>::max();',
      '            for (int k = i; k < j; k++) {',
      '                int q = dp[i][k] + dp[k + 1][j] + p[i - 1] * p[k] * p[j];',
      '                if (q < dp[i][j]) dp[i][j] = q;',
      '            }',
      '        }',
      '    }',
      '    return dp[1][n - 1];',
      '}',
    ],
    java: [
      'public class MatrixChain {',
      '    public static int solve(int[] p) {',
      '        int n = p.length;',
      '        int[][] dp = new int[n][n];',
      '        for (int len = 2; len < n; len++) {',
      '            for (int i = 1; i < n - len + 1; i++) {',
      '                int j = i + len - 1;',
      '                dp[i][j] = Integer.MAX_VALUE;',
      '                for (int k = i; k < j; k++) {',
      '                    int q = dp[i][k] + dp[k + 1][j] + p[i - 1] * p[k] * p[j];',
      '                    if (q < dp[i][j]) dp[i][j] = q;',
      '                }',
      '            }',
      '        }',
      '        return dp[1][n - 1];',
      '    }',
      '}',
    ],
    javascript: [
      'function matrixChainOrder(p) {',
      '  const n = p.length;',
      '  const dp = Array.from({ length: n }, () => new Array(n).fill(0));',
      '  for (let len = 2; len < n; len++) {',
      '    for (let i = 1; i < n - len + 1; i++) {',
      '      const j = i + len - 1;',
      '      dp[i][j] = Infinity;',
      '      for (let k = i; k < j; k++) {',
      '        const q = dp[i][k] + dp[k + 1][j] + p[i - 1] * p[k] * p[j];',
      '        if (q < dp[i][j]) dp[i][j] = q;',
      '      }',
      '    }',
      '  }',
      '  return dp[1][n - 1];',
      '}',
    ],
  },

  'coin-change': {
    pseudocode: [
      'procedure CoinChange(coins, amount)',
      '  dp ← array of size amount+1 filled with ∞',
      '  dp[0] ← 0',
      '  for i ← 1 to amount do',
      '    for each coin in coins do',
      '      if i - coin >= 0 then',
      '        dp[i] ← min(dp[i], 1 + dp[i - coin])',
      '      end if',
      '    end for',
      '  end for',
      '  return dp[amount] if dp[amount] ≠ ∞ else -1',
      'end procedure',
    ],
    python: [
      'def coin_change(coins, amount):',
      '    dp = [float("inf")] * (amount + 1)',
      '    dp[0] = 0',
      '    for i in range(1, amount + 1):',
      '        for coin in coins:',
      '            if i - coin >= 0:',
      '                dp[i] = min(dp[i], 1 + dp[i - coin])',
      '    return dp[amount] if dp[amount] != float("inf") else -1',
    ],
    cpp: [
      '#include <vector>',
      '#include <algorithm>',
      '',
      'int coinChange(const std::vector<int>& coins, int amount) {',
      '    std::vector<int> dp(amount + 1, amount + 1);',
      '    dp[0] = 0;',
      '    for (int i = 1; i <= amount; i++) {',
      '        for (int coin : coins) {',
      '            if (i - coin >= 0) dp[i] = std::min(dp[i], 1 + dp[i - coin]);',
      '        }',
      '    }',
      '    return dp[amount] > amount ? -1 : dp[amount];',
      '}',
    ],
    java: [
      'import java.util.Arrays;',
      '',
      'public class CoinChange {',
      '    public static int solve(int[] coins, int amount) {',
      '        int[] dp = new int[amount + 1];',
      '        Arrays.fill(dp, amount + 1);',
      '        dp[0] = 0;',
      '        for (int i = 1; i <= amount; i++) {',
      '            for (int coin : coins) {',
      '                if (i - coin >= 0) dp[i] = Math.min(dp[i], 1 + dp[i - coin]);',
      '            }',
      '        }',
      '        return dp[amount] > amount ? -1 : dp[amount];',
      '    }',
      '}',
    ],
    javascript: [
      'function coinChange(coins, amount) {',
      '  const dp = new Array(amount + 1).fill(Infinity);',
      '  dp[0] = 0;',
      '  for (let i = 1; i <= amount; i++) {',
      '    for (const coin of coins) {',
      '      if (i - coin >= 0) dp[i] = Math.min(dp[i], 1 + dp[i - coin]);',
      '    }',
      '  }',
      '  return dp[amount] === Infinity ? -1 : dp[amount];',
      '}',
    ],
  },

  // ════════════════════════════════════════════════════
  //  DATA STRUCTURES
  // ════════════════════════════════════════════════════
  'stack': {
    pseudocode: [
      'class Stack:',
      '  push(val): append val to top',
      '  pop(): remove and return top val',
      '  peek(): return top val without removing',
      '  isEmpty(): return size = 0',
    ],
    python: [
      'class Stack:',
      '    def __init__(self): self.items = []',
      '    def push(self, item): self.items.append(item)',
      '    def pop(self): return self.items.pop() if not self.is_empty() else None',
      '    def peek(self): return self.items[-1] if not self.is_empty() else None',
      '    def is_empty(self): return len(self.items) == 0',
    ],
    cpp: [
      '#include <vector>',
      '#include <stdexcept>',
      '',
      'template <typename T>',
      'class Stack {',
      '    std::vector<T> data;',
      'public:',
      '    void push(const T& val) { data.push_back(val); }',
      '    T pop() { T val = peek(); data.pop_back(); return val; }',
      '    T peek() const { if (isEmpty()) throw std::underflow_error("Stack empty"); return data.back(); }',
      '    bool isEmpty() const { return data.empty(); }',
      '};',
    ],
    java: [
      'import java.util.ArrayList;',
      '',
      'public class Stack<T> {',
      '    private ArrayList<T> list = new ArrayList<>();',
      '    public void push(T item) { list.add(item); }',
      '    public T pop() { T val = peek(); list.remove(list.size() - 1); return val; }',
      '    public T peek() { if (isEmpty()) throw new IllegalStateException("Empty"); return list.get(list.size() - 1); }',
      '    public boolean isEmpty() { return list.isEmpty(); }',
      '}',
    ],
    javascript: [
      'class Stack {',
      '  constructor() { this.items = []; }',
      '  push(element) { this.items.push(element); }',
      '  pop() { return this.items.pop(); }',
      '  peek() { return this.items[this.items.length - 1]; }',
      '  isEmpty() { return this.items.length === 0; }',
      '}',
    ],
  },

  'queue': {
    pseudocode: [
      'class Queue:',
      '  enqueue(val): add val to rear',
      '  dequeue(): remove and return front val',
      '  peek(): return front val without removing',
      '  isEmpty(): return size = 0',
    ],
    python: [
      'from collections import deque',
      '',
      'class Queue:',
      '    def __init__(self): self.items = deque()',
      '    def enqueue(self, item): self.items.append(item)',
      '    def dequeue(self): return self.items.popleft() if not self.is_empty() else None',
      '    def peek(self): return self.items[0] if not self.is_empty() else None',
      '    def is_empty(self): return len(self.items) == 0',
    ],
    cpp: [
      '#include <queue>',
      '#include <stdexcept>',
      '',
      'template <typename T>',
      'class CustomQueue {',
      '    std::queue<T> q;',
      'public:',
      '    void enqueue(const T& val) { q.push(val); }',
      '    T dequeue() { T val = peek(); q.pop(); return val; }',
      '    T peek() const { if (isEmpty()) throw std::underflow_error("Queue empty"); return q.front(); }',
      '    bool isEmpty() const { return q.empty(); }',
      '};',
    ],
    java: [
      'import java.util.LinkedList;',
      '',
      'public class Queue<T> {',
      '    private LinkedList<T> list = new LinkedList<>();',
      '    public void enqueue(T item) { list.addLast(item); }',
      '    public T dequeue() { return list.removeFirst(); }',
      '    public T peek() { return list.getFirst(); }',
      '    public boolean isEmpty() { return list.isEmpty(); }',
      '}',
    ],
    javascript: [
      'class Queue {',
      '  constructor() { this.items = []; }',
      '  enqueue(element) { this.items.push(element); }',
      '  dequeue() { return this.items.shift(); }',
      '  peek() { return this.items[0]; }',
      '  isEmpty() { return this.items.length === 0; }',
      '}',
    ],
  },

  'linked-list': {
    pseudocode: [
      'class LinkedList:',
      '  insertAtHead(val): create Node(val), point to current head',
      '  deleteVal(val): bypass node with value val',
      '  search(val): traverse pointers until match',
    ],
    python: [
      'class Node:',
      '    def __init__(self, data):',
      '        self.data = data',
      '        self.next = None',
      '',
      'class LinkedList:',
      '    def __init__(self): self.head = None',
      '    def insert_head(self, data):',
      '        new_node = Node(data)',
      '        new_node.next = self.head',
      '        self.head = new_node',
      '    def delete_val(self, val):',
      '        curr = self.head; prev = None',
      '        while curr:',
      '            if curr.data == val:',
      '                if prev: prev.next = curr.next',
      '                else: self.head = curr.next',
      '                return True',
      '            prev = curr; curr = curr.next',
      '        return False',
    ],
    cpp: [
      'struct Node {',
      '    int data;',
      '    Node* next;',
      '    Node(int val) : data(val), next(nullptr) {}',
      '};',
      '',
      'class LinkedList {',
      '    Node* head = nullptr;',
      'public:',
      '    void insertHead(int val) {',
      '        Node* n = new Node(val);',
      '        n->next = head; head = n;',
      '    }',
      '    bool deleteVal(int val) {',
      '        Node *curr = head, *prev = nullptr;',
      '        while (curr) {',
      '            if (curr->data == val) {',
      '                if (prev) prev->next = curr->next;',
      '                else head = curr->next;',
      '                delete curr; return true;',
      '            }',
      '            prev = curr; curr = curr->next;',
      '        }',
      '        return false;',
      '    }',
      '};',
    ],
    java: [
      'class Node {',
      '    int data;',
      '    Node next;',
      '    Node(int d) { data = d; }',
      '}',
      '',
      'public class LinkedList {',
      '    private Node head;',
      '    public void insertHead(int val) {',
      '        Node n = new Node(val);',
      '        n.next = head; head = n;',
      '    }',
      '    public boolean deleteVal(int val) {',
      '        Node curr = head, prev = null;',
      '        while (curr != null) {',
      '            if (curr.data == val) {',
      '                if (prev != null) prev.next = curr.next;',
      '                else head = curr.next;',
      '                return true;',
      '            }',
      '            prev = curr; curr = curr.next;',
      '        }',
      '        return false;',
      '    }',
      '}',
    ],
    javascript: [
      'class Node {',
      '  constructor(data) { this.data = data; this.next = null; }',
      '}',
      '',
      'class LinkedList {',
      '  constructor() { this.head = null; }',
      '  insertHead(data) {',
      '    const newNode = new Node(data);',
      '    newNode.next = this.head; this.head = newNode;',
      '  }',
      '  deleteVal(val) {',
      '    let curr = this.head, prev = null;',
      '    while (curr) {',
      '      if (curr.data === val) {',
      '        if (prev) prev.next = curr.next;',
      '        else this.head = curr.next;',
      '        return true;',
      '      }',
      '      prev = curr; curr = curr.next;',
      '    }',
      '    return false;',
      '  }',
      '}',
    ],
  },

  'hash-table': {
    pseudocode: [
      'class HashTable:',
      '  hash(key): return key.hashCode() % capacity',
      '  put(key, value): buckets[hash(key)].append((key, value))',
      '  get(key): search bucket for matching key',
    ],
    python: [
      'class HashTable:',
      '    def __init__(self, size=10):',
      '        self.size = size',
      '        self.table = [[] for _ in range(size)]',
      '',
      '    def _hash(self, key): return hash(key) % self.size',
      '    def put(self, key, value):',
      '        idx = self._hash(key)',
      '        for i, (k, v) in enumerate(self.table[idx]):',
      '            if k == key: self.table[idx][i] = (key, value); return',
      '        self.table[idx].append((key, value))',
      '    def get(self, key):',
      '        idx = self._hash(key)',
      '        for k, v in self.table[idx]:',
      '            if k == key: return v',
      '        return None',
    ],
    cpp: [
      '#include <vector>',
      '#include <list>',
      '#include <string>',
      '',
      'class HashTable {',
      '    int size;',
      '    std::vector<std::list<std::pair<std::string, int>>> table;',
      '    int hash(std::string key) const {',
      '        int h = 0; for (char c : key) h = (h * 31 + c) % size; return h;',
      '    }',
      'public:',
      '    HashTable(int s = 10) : size(s), table(s) {}',
      '    void put(std::string key, int val) {',
      '        int idx = hash(key);',
      '        for (auto& pair : table[idx]) if (pair.first == key) { pair.second = val; return; }',
      '        table[idx].push_back({key, val});',
      '    }',
      '};',
    ],
    java: [
      'import java.util.LinkedList;',
      '',
      'public class HashTable<K, V> {',
      '    static class Entry<K, V> { K key; V val; Entry(K k, V v) { key = k; val = v; } }',
      '    private LinkedList<Entry<K, V>>[] buckets;',
      '    public HashTable(int cap) { buckets = new LinkedList[cap]; }',
      '    public void put(K key, V val) {',
      '        int idx = Math.abs(key.hashCode()) % buckets.length;',
      '        if (buckets[idx] == null) buckets[idx] = new LinkedList<>();',
      '        for (Entry<K, V> e : buckets[idx]) if (e.key.equals(key)) { e.val = val; return; }',
      '        buckets[idx].add(new Entry<>(key, val));',
      '    }',
      '}',
    ],
    javascript: [
      'class HashTable {',
      '  constructor(size = 10) {',
      '    this.size = size;',
      '    this.buckets = Array.from({ length: size }, () => []);',
      '  }',
      '  _hash(key) {',
      '    let hash = 0;',
      '    for (let i = 0; i < key.length; i++) hash = (hash * 31 + key.charCodeAt(i)) % this.size;',
      '    return hash;',
      '  }',
      '  put(key, value) {',
      '    const idx = this._hash(key);',
      '    const bucket = this.buckets[idx];',
      '    const match = bucket.find(item => item[0] === key);',
      '    if (match) match[1] = value;',
      '    else bucket.push([key, value]);',
      '  }',
      '}',
    ],
  },

  'union-find': {
    pseudocode: [
      'class UnionFind(n):',
      '  find(i): path compression to find root',
      '  union(i, j): connect components by rank',
      '  connected(i, j): return find(i) = find(j)',
    ],
    python: [
      'class UnionFind:',
      '    def __init__(self, n):',
      '        self.parent = list(range(n))',
      '        self.rank = [1] * n',
      '    def find(self, i):',
      '        if self.parent[i] == i: return i',
      '        self.parent[i] = self.find(self.parent[i])  # Path compression',
      '        return self.parent[i]',
      '    def union(self, i, j):',
      '        r_i, r_j = self.find(i), self.find(j)',
      '        if r_i != r_j:',
      '            if self.rank[r_i] < self.rank[r_j]: r_i, r_j = r_j, r_i',
      '            self.parent[r_j] = r_i',
      '            if self.rank[r_i] == self.rank[r_j]: self.rank[r_i] += 1',
      '            return True',
      '        return False',
    ],
    cpp: [
      '#include <vector>',
      '#include <numeric>',
      '',
      'class UnionFind {',
      '    std::vector<int> parent, rank;',
      'public:',
      '    UnionFind(int n) : parent(n), rank(n, 1) {',
      '        std::iota(parent.begin(), parent.end(), 0);',
      '    }',
      '    int find(int i) { return parent[i] == i ? i : parent[i] = find(parent[i]); }',
      '    bool unite(int i, int j) {',
      '        int rI = find(i), rJ = find(j);',
      '        if (rI != rJ) {',
      '            if (rank[rI] < rank[rJ]) std::swap(rI, rJ);',
      '            parent[rJ] = rI;',
      '            if (rank[rI] == rank[rJ]) rank[rI]++;',
      '            return true;',
      '        }',
      '        return false;',
      '    }',
      '};',
    ],
    java: [
      'public class UnionFind {',
      '    private int[] parent, rank;',
      '    public UnionFind(int n) {',
      '        parent = new int[n]; rank = new int[n];',
      '        for (int i = 0; i < n; i++) { parent[i] = i; rank[i] = 1; }',
      '    }',
      '    public int find(int i) { return parent[i] == i ? i : (parent[i] = find(parent[i])); }',
      '    public boolean union(int i, int j) {',
      '        int rI = find(i), rJ = find(j);',
      '        if (rI != rJ) {',
      '            if (rank[rI] < rank[rJ]) { int temp = rI; rI = rJ; rJ = temp; }',
      '            parent[rJ] = rI;',
      '            if (rank[rI] == rank[rJ]) rank[rI]++;',
      '            return true;',
      '        }',
      '        return false;',
      '    }',
      '}',
    ],
    javascript: [
      'class UnionFind {',
      '  constructor(n) {',
      '    this.parent = Array.from({ length: n }, (_, i) => i);',
      '    this.rank = new Array(n).fill(1);',
      '  }',
      '  find(i) {',
      '    if (this.parent[i] === i) return i;',
      '    return (this.parent[i] = this.find(this.parent[i]));',
      '  }',
      '  union(i, j) {',
      '    let rI = this.find(i), rJ = this.find(j);',
      '    if (rI !== rJ) {',
      '      if (this.rank[rI] < this.rank[rJ]) [rI, rJ] = [rJ, rI];',
      '      this.parent[rJ] = rI;',
      '      if (this.rank[rI] === this.rank[rJ]) this.rank[rI]++;',
      '      return true;',
      '    }',
      '    return false;',
      '  }',
      '}',
    ],
  },
};
